---
layout: post
title: 파이썬 - 문자열 자료형
date: 2024-07-29 19:20:23 +0900
category: Python
---
# 문자열 자료형

## 1. 문자열 정의

파이썬에서 문자열은 작은 따옴표(`'`) 또는 큰 따옴표(`"`)로 감싸서 표현합니다.  
문자열은 **불변**이므로 값을 변경하려면 항상 **새 문자열을 만들어 재할당**합니다.

```python
# 문자열 정의 예시
string1 = 'Hello, World!'
string2 = "Python is awesome"

# 불변성 확인(재할당 시 다른 객체)
string1 = 'Hello, World!'
print(id(string1))
string1 = "Python is awesome"
print(id(string1))        # 다른 id (새 객체)
```

> 불변(immutable) → **슬라이싱/치환 시 항상 새 객체 생성**. 제자리 수정(in-place)은 불가.

---

## 2. 여러 줄 문자열

삼중 따옴표(`''' ... '''` 또는 `""" ... """`)는 줄바꿈과 따옴표를 그대로 포함할 수 있습니다.

```python
multiline = '''This is
a multiline
string.'''
print(multiline)
```

### (보강) 들여쓰기 제거: `textwrap.dedent`

문서 문자열을 코드 안에 들여써야 한다면, `dedent`로 좌측 공백을 정리합니다.

```python
from textwrap import dedent

sql = dedent("""
    SELECT id, name
    FROM users
    WHERE active = 1
""").strip()
print(sql.splitlines())
```

---

## 3. 문자열 연결 및 반복

- **연결(Concatenation)**: `+`
- **반복(Repetition)**: `*`

```python
greeting = "Hello, " + "World!"       # 'Hello, World!'
repeat = "A" * 5                      # 'AAAAA'
```

### (보강) 성능 팁: `''.join(iterable)` 권장

루프에서 `+`로 누적 연결은 **매번 새 객체**를 만들어 **O(n^2)** 로 악화됩니다.  
리스트에 쌓아두고 마지막에 `''.join(...)`으로 결합하세요.

```python
parts = []
for i in range(5):
    parts.append(str(i))
s = ",".join(parts)   # '0,1,2,3,4'
```

---

## 4. 문자열 인덱싱 및 슬라이싱

- 문자열은 **0부터 시작**하는 인덱스를 가집니다.
- 슬라이싱: `[시작:끝:스텝]` (끝은 **포함하지 않음**)

```python
text = "Python"
print(text[0])      # 'P'
print(text[-1])     # 'n'
print(text[1:4])    # 'yth'  (1 ≤ idx < 4)
print(text[::-1])   # 'nohtyP'  (역순)
print(text[::2])    # 'Pto' (격자 샘플링)
```

> 슬라이싱은 새 문자열을 반환(불변성). **슬라이스 대입은 불가**합니다.

---

## 5. 문자열 메서드

### 5.1 대소문자 변환/비교

```python
text = "Straße"               # 독일어 ß
print(text.upper())           # 'STRASSE' (언어별 규칙 차이 가능)
print(text.lower())           # 'straße'
print(text.capitalize())      # 'Straße'
print(text.title())           # 'Straße'
print(text.casefold())        # 'strasse' (강한 비교용)
print("straße".casefold() == "STRASSE".casefold())  # True
```

- **`casefold()`**: 국제화(i18n) 문자열 **비교용** 강한 소문자화.

### 5.2 검색/개수/접두사·접미사

```python
text = "banana"
print(text.find("a"))            # 1 (없으면 -1)
print(text.rfind("a"))           # 5
print(text.index("na"))          # 2 (없으면 ValueError)
print(text.count("na"))          # 2

print(text.startswith("ba"))     # True
print(text.endswith("na"))       # True
```

### 5.3 공백/문자 제거 계열

```python
text = "   Hello, World!   "
print(text.strip())              # 양끝 공백 제거
print(text.lstrip())             # 왼쪽만
print(text.rstrip())             # 오른쪽만

# 특정 문자 집합 제거
print("---abc---".strip("-"))    # 'abc'
```

### 5.4 치환/분리/결합

```python
print("I like Python".replace("Python", "Java"))  # 'I like Java'

text = "apple,banana,orange"
print(text.split(","))           # ['apple','banana','orange']
print(text.rsplit(",", 1))       # ['apple,banana', 'orange']
print("a\nb\nc".splitlines())    # ['a','b','c']

print(" ".join(['Hello', 'World']))  # 'Hello World'
```

### 5.5 정렬/채우기/패딩

```python
print("hi".center(8, "-"))       # '--hi----'
print("42".zfill(5))             # '00042'
print("hi".ljust(6, "."))        # 'hi....'
print("hi".rjust(6, "."))        # '....hi'
```

### 5.6 `partition` / `rpartition`

```python
print("key=value".partition("="))     # ('key', '=', 'value')
print("a-b-c".rpartition("-"))        # ('a-b', '-', 'c')
```

### 5.7 번역 테이블: `str.maketrans` + `translate`

```python
tbl = str.maketrans({"-": "_", " ": ""})
print("hello-world example".translate(tbl))  # 'hello_worldexample'
```

### 5.8 분류 `is*` 메서드

```python
print("123".isdecimal(), "Ⅳ".isdigit(), "A".isalpha())   # True True True
print("abc123".isalnum(), "   ".isspace())               # True True
print("Title Case".istitle())                            # True
```

> `isdecimal ⊂ isdigit ⊂ isnumeric` 차이를 이해하면 국제 숫자 처리에 유용.

---

## 6. 문자열 포매팅

### 6.1 f-string (권장, Py3.6+)

```python
name, age = "Alice", 25
print(f"My name is {name} and I am {age} years old.")
pi = 3.1415926535
print(f"{pi:.3f}")            # 3.142
print(f"{255:#06x}")          # '0x00ff'
print(f"{0.125:.2%}")         # '12.50%'
```

- **포맷 미니언어**: `[{값:필드}]`
  - 정렬/폭/채움: `:<`, `:>`, `:^`, 예: `f"{'hi':->8}"`
  - 숫자: 정밀도/지수/천단위: `:.2f`, `:.3e`, `:,`
  - 진법: `b`, `o`, `x`, `X` + `#`(접두어)

### 6.2 `str.format()`

```python
print("name={0}, age={1}".format("Bob", 30))
print("x={x}, y={y}".format(x=1, y=2))
print("{:,.2f}".format(1234567.89))   # '1,234,567.89'
```

### 6.3 `%` 포매팅(레거시)

| 포맷 | 설명 | 예시 |
|---|---|---|
| `%d`/`%i` | 10진 정수 | `"%d" % 42` |
| `%o`/`%x`/`%X` | 8/16진수 | `"%x" % 42` → `2a` |
| `%e`/`%E` | 지수 | `"%.2e" % 42.42` |
| `%f`/`%F` | 고정소수 | `"%.2f" % 42.42` |
| `%g`/`%G` | 자동 | `"%.2g" % 42.42` |
| `%c` | 문자 | `"%c" % 65` → `'A'` |
| `%s` | 문자열 | `"%s" % "hi"` |
| `%%` | `%` 자체 | `"%%" % ()` |

```python
print("My name is %s and I am %d years old." % ("Charlie", 35))
print("%10s" % "hi")          # '        hi'
print("%-10s" % "hi")         # 'hi        '
print("%10.4f" % 3.14123123)  # '    3.1412'
```

### 6.4 템플릿 문자열: `string.Template` (간단/안전)

```python
from string import Template
t = Template("Hello, $name! Today is $day.")
print(t.substitute(name="Jin", day="Monday"))
```

---

## 7. 문자열의 불변성

문자열은 생성 후 수정이 불가능합니다. 값을 “바꾸려면” **새 문자열을 조합**해야 합니다.

```python
text = "Hello"
# text[0] = 'h'  # TypeError
text = "h" + text[1:]
print(text)  # 'hello'
```

---

## 8. 문자열과 인코딩

파이썬 `str`은 **유니코드 코드 포인트**의 시퀀스입니다.  
**바이트 변환**은 `encode()`, **역변환**은 `decode()`를 사용합니다.

```python
text = "안녕하세요"
encoded = text.encode("utf-8")      # bytes
print(encoded)                       # b'\xec\x95\x88...'

decoded = encoded.decode("utf-8")    # str
print(decoded)                       # '안녕하세요'
```

### 8.1 에러 처리 전략

```python
print("😀".encode("latin-1", errors="ignore"))   # b'' (무시)
print("😀".encode("latin-1", errors="replace"))  # b'?' 대체
print("😀".encode("utf-8").decode("utf-8"))      # 원복 가능
```

### 8.2 원시 문자열 `r"..."` (백슬래시 이스케이프 무시)

```python
path = r"C:\Users\name\docs"   # 백슬래시를 그대로
# 단, 마지막이 홀수 개의 백슬래시로 끝나면 문법 오류
```

---

## 9. 문자열 연산자

| 연산자   | 기능                 | 예시                                 |
|---------|----------------------|--------------------------------------|
| `+`     | 연결                 | `"a" + "b"` → `'ab'`                 |
| `*`     | 반복                 | `"a" * 3` → `'aaa'`                  |
| `in`    | 포함 여부            | `"a" in "apple"` → `True`            |
| `not in`| 포함 여부(부정)      | `"x" not in "apple"` → `True`        |
| `==`    | 값 비교              | `"a" == "a"` → `True`                |
| `<` 등  | 사전식(lexicographic)| `"abc" < "abd"` → `True`             |

---

## 10. 이스케이프 시퀀스

| 시퀀스 | 설명 |
|---|---|
| `\n` | 새 줄 |
| `\t` | 탭 |
| `\\` | 백슬래시 |
| `\'` / `\"` | 작은/큰따옴표 |
| `\r` | 캐리지 리턴 |
| `\f` | 폼 피드 |
| `\a` | 벨(비프) |
| `\b` | 백스페이스 |
| `\000` | 널 문자 |

```python
text = "Hello\nWorld"
print(text)
# Hello
# World
```

---

## 11. 유니코드 심화 (정규화/길이/조합문자)

같은 글자처럼 보이지만, **코드 포인트**가 다른 경우가 있습니다(예: 조합형 악센트).

```python
s1 = "é"                   # U+00E9 (단일 코드 포인트)
s2 = "e\u0301"             # 'e' + COMBINING ACUTE ACCENT
print(s1 == s2)            # False (정규화 필요)
print(len(s1), len(s2))    # 1, 2
```

### 11.1 유니코드 정규화(`unicodedata.normalize`)

```python
import unicodedata as ud
def norm(s): return ud.normalize("NFC", s)   # 또는 NFD/NFKC/NFKD
print(norm(s1) == norm(s2))                  # True
```

### 11.2 그래프림(사용자 인지 글자) 단위

파이썬 `len()`은 **코드 포인트 수**를 반환합니다.  
이모지/조합문자/한글 자모 조합은 시각적 “글자 수”와 다를 수 있습니다.  
정확한 그래프림 처리는 `regex` 모듈(サード파티)의 `\X` 패턴처럼 **전용 라이브러리**가 필요합니다.

---

## 12. 정규표현식과 문자열 (요약)

간단 치환은 `str.replace`, 패턴 기반은 `re` 사용:

```python
import re

s = "a1 b22 c333"
print(re.findall(r"\d+", s))           # ['1','22','333']
print(re.sub(r"\s+", "-", s))          # 'a1-b22-c333'

m = re.search(r"b(\d+)", s)
print(m.group(1))                      # '22'
```

> 정규식은 **가독성과 성능**을 고려해 사용. **단순한 경우엔 문자열 메서드가 더 빠르고 명확**합니다.

---

## 13. 보안/성능 팁

### 13.1 상수 시간 비교: `hmac.compare_digest`
민감 문자열(토큰/서명) 비교 시 **타이밍 누출** 방지를 위해:

```python
import hmac
print(hmac.compare_digest("abc", "abd"))  # False (시간 상수)
```

### 13.2 대량 문자열 구성

- 누적 연결 대신 `''.join(list)` 또는 `io.StringIO` 사용.

```python
from io import StringIO
buf = StringIO()
for i in range(3):
    buf.write(str(i))
s = buf.getvalue()      # '012'
```

### 13.3 인터닝(interning)으로 비교 가속

```python
import sys
a = sys.intern("very_long_key")
b = sys.intern("very_long_key")
print(a is b)           # True (동일 객체 → 빠른 비교/딕셔너리 키 최적화)
```

---

## 14. 실전 시나리오

### 14.1 로그 라인 파싱 → 딕셔너리

```python
line = "ts=2025-11-10 level=INFO user=alice action=login"
def parse_kv(line):
    out = {}
    for part in line.split():
        if "=" in part:
            k, v = part.split("=", 1)
            out[k] = v
    return out

print(parse_kv(line))   # {'ts':'2025-11-10','level':'INFO','user':'alice','action':'login'}
```

### 14.2 안전한 경로 결합 (윈도우 백슬래시 주의)

```python
# 문자열 자체로는 경로 인식이 없음 → pathlib 사용을 권장
from pathlib import Path

base = Path(r"C:\data")
p = base / "logs" / "app.log"
print(str(p))           # C:\data\logs\app.log
```

### 14.3 국제화 비교: `casefold` + 정규화

```python
import unicodedata as ud

def i18n_equal(a, b):
    norm = lambda s: ud.normalize("NFC", s).casefold()
    return norm(a) == norm(b)

print(i18n_equal("Straße", "STRASSE"))  # True
```

### 14.4 CSV 안전 결합

```python
# 직접 'a,b,c' 만들지 말고 csv 모듈 사용(인용/이스케이프 자동)
import csv, io
buf = io.StringIO()
writer = csv.writer(buf)
writer.writerow(["Alice, Inc.", "a\"b", 42])
print(buf.getvalue())   # "Alice, Inc.","a""b",42
```

---

## 15. 체크리스트 요약

- **불변**: 수정 불가, 새 객체 생성. 루프 연결은 `join`/`StringIO`.
- **서식**: f-string + 포맷 미니언어로 폭/정렬/숫자 서식 제어.
- **검색/치환**: 단순 → 문자열 메서드, 패턴 → `re`.
- **유니코드**: 정규화(NFC 등)와 `casefold()`로 국제화 안전 비교.
- **인코딩**: `encode`/`decode` + `errors` 전략 명시.
- **보안**: 민감 비교는 `hmac.compare_digest`.

---

## 16. 빠른 실습 스니펫

```python
# 1) 역순/슬라이스
print("stressed"[::-1])   # 'desserts'

# 2) 줄 단위 처리
text = "a\r\nb\nc"
for line in text.splitlines():
    print(repr(line))

# 3) 접두사/접미사 제거(파이썬 3.9+)
print("prefix_value".removeprefix("prefix_"))  # 'value'
print("file.txt".removesuffix(".txt"))         # 'file'

# 4) 패딩 + 정렬
print(f"[{'title':^20}]")   # 중앙 정렬 폭 20

# 5) 안전 비교
import hmac
print(hmac.compare_digest("token123", "token124"))

# 6) 글자수와 표시폭은 다를 수 있음(이모지/조합문자)
s = "👨‍👩‍👧‍👦"       # 가족 이모지(여러 코드포인트 조합)
print(len(s))            # 코드포인트 수
```

---

# 마무리

- 본 정리는 사용자가 제시한 구조와 내용을 유지하면서, **문자열 메서드 전반, 포맷 미니언어, 유니코드 정규화, 성능/보안 팁**을 추가했습니다.
- 같은 스타일(# 제목 → ## 세부)로 다른 파트(리스트/딕셔너리/튜플 등)도 동일 기준으로 확장 가능합니다.