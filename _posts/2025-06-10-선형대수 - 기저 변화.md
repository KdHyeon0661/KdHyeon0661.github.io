---
layout: post
title: 선형대수 - 기저 변화
date: 2025-06-10 22:20:23 +0900
category: 선형대수
---
# 🔁 기저 변화 (Change of Basis)

> **핵심 요약 (TL;DR)**
> - **벡터는 불변, 좌표만 변함.** 기저가 바뀌면 같은 벡터의 좌표표현이 달라진다.
> - **변환행렬(좌표변환행렬)**:  \(P_{C\leftarrow B} = C^{-1}B\).  (여기서 \(B=[\boldsymbol b_1\ \cdots\ \boldsymbol b_n]\), \(C=[\boldsymbol c_1\ \cdots\ \boldsymbol c_n]\))
> - **선형변환의 유사변환**:  표준기저 행렬 \(A\)에 대해, **기저 \(B\)** 기준 행렬은 \(A_B = B^{-1}AB\).
>   두 기저 \(B\), \(C\) 사이에서는 \(A_C = (C^{-1}B)^{-1}A_B(C^{-1}B) = P_{B\leftarrow C}\,A_B\,P_{C\leftarrow B}\).
> - **투영·대각화·PCA** 등: “좋은 기저”로 바꾸면 계산이 **단순**·**안정**·**설명력↑**.

---

## 1. 기저와 좌표 — “벡터 vs 벡터의 좌표”

- \(V=\mathbb{R}^n\) 에서 **기저** \(B=\{\boldsymbol b_1,\dots,\boldsymbol b_n\}\) 는
  모든 \(\boldsymbol x\in V\)를 **유일하게** \(\boldsymbol x=\sum_i \alpha_i \boldsymbol b_i\)로 표현하게 해주는 최소 벡터집합.
- **벡터 자체** \(\boldsymbol x\)는 기저와 무관하게 **하나**지만,
  **좌표** \([\boldsymbol x]_B = (\alpha_1,\dots,\alpha_n)^\top\) 는 **기저에 따라 달라짐**.

> 표기 관례
> - 기저행렬 \(B\in\mathbb{R}^{n\times n}\): 열에 \(\boldsymbol b_i\)를 둔 행렬 \(B=[\boldsymbol b_1\ \cdots\ \boldsymbol b_n]\).
> - 표준기저 \(E\)에서는 \([\boldsymbol x]_E=\boldsymbol x\) (그냥 좌표값).
> - 언제나 \(\boldsymbol x = B\,[\boldsymbol x]_B = C\,[\boldsymbol x]_C\).

---

## 2. 좌표변환행렬 \(P_{C\leftarrow B}\) — 벡터 좌표를 B→C로

\[
\boldsymbol x = B[\boldsymbol x]_B = C[\boldsymbol x]_C
\ \Rightarrow\
[\boldsymbol x]_C = C^{-1}B\,[\boldsymbol x]_B
\]

\[
\boxed{\,P_{C\leftarrow B} \;=\; C^{-1}B\,}
\qquad
\boxed{\,P_{B\leftarrow C} \;=\; B^{-1}C \;=\; (P_{C\leftarrow B})^{-1}\,}
\]

- **조건**: \(B, C\)는 **가역**(정사각·열독립)해야 함.
- **직교기저**(예: \(Q\)가 직교행렬)라면 \(Q^{-1}=Q^\top\) → 역이 손쉬움.

---

## 3. 선형변환의 기저 변화 — 유사변환(자료형 불변, 표현만 변환)

선형변환 \(T:V\to V\)의 **표준기저 표현**을 \(A\)라 하면
\[
\boldsymbol y = A\,\boldsymbol x
\]
- **기저 \(B\) 기준** 행렬 \(A_B\) (곧, \([\boldsymbol y]_B = A_B[\boldsymbol x]_B\)):
\[
B[\boldsymbol y]_B = A\,B[\boldsymbol x]_B
\ \Rightarrow\
[\boldsymbol y]_B = \underbrace{B^{-1}AB}_{A_B}[\boldsymbol x]_B
\quad\Rightarrow\quad
\boxed{\,A_B = B^{-1}AB\,}
\]
- **두 기저 \(B\), \(C\)** 사이 관계:
\[
\boxed{\,A_C = P_{B\leftarrow C}\,A_B\,P_{C\leftarrow B} \;=\; (C^{-1}B)^{-1}A_B(C^{-1}B)\,}
\]
즉 \(A_C\)와 \(A_B\)는 **유사행렬**. → **고윳값은 동일** (기저에 무관).

---

## 4. 기하/수치 관점에서의 의미

- **좋은 기저 = 쉬운 해석**
  - 고유기저 → **대각화**(\(A=S\Lambda S^{-1}\))로 계산 단순화.
  - 정규직교 기저 → 내적/노름 보존, 수치안정.
- **PCA**: 데이터 공분산의 고유벡터 기저로 표현 → **상관 제거** & **차원축소**.
- **그래픽스**: 월드/뷰/로컬 좌표계 간 변환 = 기저 변화.
- **제어/신호**: 모드 분해(고유기저)로 **모드별 분석**.

---

## 5. 예제 A — 2D 좌표계 B↔C, 벡터 좌표 변환

### 5.1 기저 정의
\[
B=
\begin{bmatrix}
1&1\\
1&-1
\end{bmatrix},\qquad
C=
\begin{bmatrix}
2&0\\
1&1
\end{bmatrix}
\quad(\text{각 열이 기저벡터})
\]

### 5.2 변환행렬
\[
P_{C\leftarrow B} = C^{-1}B,\qquad P_{B\leftarrow C}=B^{-1}C=(P_{C\leftarrow B})^{-1}
\]

### 5.3 벡터 변환
\[
[\boldsymbol x]_B = \begin{bmatrix}1\\1\end{bmatrix}
\ \Rightarrow\
[\boldsymbol x]_C = P_{C\leftarrow B}[\boldsymbol x]_B
\]
검증: \(\boldsymbol x = B[{\boldsymbol x}]_B = C[{\boldsymbol x}]_C\).

#### Python (NumPy)
```python
import numpy as np

B = np.array([[1., 1.],
              [1.,-1.]])
C = np.array([[2., 0.],
              [1., 1.]])

PC_B = np.linalg.inv(C) @ B      # P_{C<-B}
PB_C = np.linalg.inv(B) @ C      # P_{B<-C}

x_B = np.array([1., 1.])         # [x]_B
x_C = PC_B @ x_B                 # [x]_C
x_E1 = B @ x_B                   # x in standard coords
x_E2 = C @ x_C                   # must equal x_E1

print("P_{C<-B}=\n", PC_B)
print("[x]_C =", x_C)
print("x from B =", x_E1, " ; x from C =", x_E2)
print("Consistency? ", np.allclose(x_E1, x_E2))
```

---

## 6. 예제 B — 선형변환 행렬의 기저 변화 (유사변환)

- 표준기저에서의 선형변환:
\[
A=\begin{bmatrix}
3&1\\
0&2
\end{bmatrix}
\]
- 기저 \(B\) (위 5.1과 동일) 기준 행렬:
\[
A_B = B^{-1}AB
\]
- 기저 \(C\) 기준 행렬:
\[
A_C = C^{-1}AC
\]
- 직접 \(B\to C\)로도 가능:
\[
A_C = P_{B\leftarrow C} A_B P_{C\leftarrow B}
\]

#### Python
```python
import numpy as np

A = np.array([[3.,1.],
              [0.,2.]])

B = np.array([[1., 1.],
              [1.,-1.]])
C = np.array([[2., 0.],
              [1., 1.]])

A_B = np.linalg.inv(B) @ A @ B
A_C = np.linalg.inv(C) @ A @ C

PC_B = np.linalg.inv(C) @ B       # P_{C<-B}
PB_C = np.linalg.inv(B) @ C       # P_{B<-C}

A_C_via_AB = PB_C @ A_B @ PC_B

print("A_B=\n", A_B)
print("A_C (direct)=\n", A_C)
print("A_C (via A_B)=\n", A_C_via_AB)
print("Match? ", np.allclose(A_C, A_C_via_AB))
print("eig(A) == eig(A_B) == eig(A_C)?")
print(np.linalg.eigvals(A), np.linalg.eigvals(A_B), np.linalg.eigvals(A_C))
```

---

## 7. 예제 C — **대각화**: 고유기저로의 기저 변화

- \(A\)가 대각화 가능하면 \(A = S\Lambda S^{-1}\) (열: 고유벡터).
- 고유기저 \(B=S\)로 바꾸면
\[
A_B = B^{-1}AB = S^{-1} A S = \Lambda
\]
→ 계산이 **독립 스칼라 문제**로 분해됨.

#### Python
```python
import numpy as np

A = np.array([[4., 1., 0.],
              [0., 3., 0.],
              [0., 0., 2.]])

# 여긴 이미 상삼각(여기선 사실 대각에 가깝다). 일반 행렬이면 np.linalg.eig로 S, Lambda 구함.
eigvals, S = np.linalg.eig(A)    # A S = S diag(eig)
Lambda = np.diag(eigvals)
A_in_eig_basis = np.linalg.inv(S) @ A @ S

print("diag(eig) =\n", Lambda)
print("S^{-1} A S =\n", A_in_eig_basis)
print("Diagonalized? ", np.allclose(A_in_eig_basis, Lambda))
```

> **포인트**: 같은 선형변환이라도 **기저를 고유기저로** 바꾸면 행렬이 대각이 되어
> 거듭제곱, 지수행렬, 미분방정식 해석 등이 **극적으로 단순화**된다.

---

## 8. 예제 D — **PCA 좌표계**로의 기저 변화 (데이터 분석 관점)

- 데이터 행렬 \(X\in\mathbb{R}^{m\times n}\) (행: 표본, 열: 특징)을 평균 제거 후 SVD:
  \[
  X = U\Sigma V^\top
  \]
  여기서 \(V\)의 열이 **주성분 방향**(고유기저).
- 좌표를 **PCA 기저**로 바꾸면: \([\boldsymbol x]_{\text{PCA}} = V^\top \boldsymbol x\).
- 상관 제거(공분산 대각화), 저차원 투영이 쉬워짐.

#### Python
```python
import numpy as np

rng = np.random.default_rng(0)
X = rng.normal(size=(200, 3))
X[:,2] = X[:,0] + 0.5*X[:,1] + 0.01*rng.normal(size=200)  # 상관 도입

Xc = X - X.mean(axis=0)
U, S, VT = np.linalg.svd(Xc, full_matrices=False)         # Xc = U Σ V^T
V = VT.T
X_pca = Xc @ V                                            # 표본을 PCA 기저로 표현

# 공분산이 대각형태?
cov_pca = np.cov(X_pca, rowvar=False)
print("Cov in PCA basis (≈ diagonal):\n", np.round(cov_pca, 4))
```

---

## 9. (중요) **좌표변환 vs. 능동변환(Active) 혼동 금지**

- **좌표변환(수동)**: **벡터는 고정**, 다만 그 **표현(좌표)이 바뀜**. \( [\boldsymbol x]_C = P_{C\leftarrow B}[\boldsymbol x]_B \).
- **능동변환(예: 회전)**: **벡터 자체**를 바꾼다(새 벡터 \(\boldsymbol x' = R\boldsymbol x\)).
  같은 상황을 두 관점에서 쓸 수 있으나, 행렬의 위치가 바뀜에 유의.

> 실무 팁: “좌표계를 바꿨다(수동)” vs “벡터를 회전시켰다(능동)”를 명확히 구분하면
> 코드를 짤 때 \(R\), \(R^{-1}\), \(R^\top\) 중 무엇을 곱할지 헷갈리지 않는다.

---

## 10. (심화) **내적·그래프(Gram) 행렬**과 기저 변화

- 표준내적 \(\langle \boldsymbol x,\boldsymbol y\rangle = \boldsymbol x^\top \boldsymbol y\) 에서
  기저 \(B\) 좌표로 쓰면
  \[
  \boldsymbol x = B[\boldsymbol x]_B,\quad
  \langle \boldsymbol x,\boldsymbol y\rangle
  = [\boldsymbol x]_B^\top \underbrace{(B^\top B)}_{G_B} [\boldsymbol y]_B
  \]
  여기서 \(G_B\)가 **Gram 행렬**.
- **정규직교 기저**면 \(G_B=I\)라서 좌표계에서 내적이 단순.

---

## 11. (심화) **선형함수(공변량, covector)**와 **쌍대기저**

- 선형함수 \(\alpha:V\to\mathbb{R}\)는 **행벡터**로 나타낸다.
  기저 \(B\)에서 \([\alpha]_B^\top\)이면, 표준좌표에선 \(\alpha_E^\top = [\alpha]_B^\top B^{-1}\).
- 기저 변화 \(B\to C\)에서
  \[
  [\alpha]_C^\top = [\alpha]_B^\top P_{B\leftarrow C}
  \]
  (**열벡터와 반대로** 변환: “역전치” 이슈 주의).

---

## 12. 자주 하는 실수 & 베스트 프랙티스

1. **기저행렬의 열**에 기저벡터 배치할 것(가장 일반적이고 \(x = B[x]_B\)가 자연스러움).
2. \(P_{C\leftarrow B}=C^{-1}B\) 공식을 **통째로** 기억하되, **유도**도 익혀 두기(실수 방지).
3. 선형변환의 기저 변화는 **유사변환** \(A_B=B^{-1}AB\)임을 기억(고윳값 불변).
4. 수치해석에서는 **직교기저**(QR, SVD)로 바꿔서 계산 안정성 확보.
5. 좌표(수동) vs 능동 변환(진짜로 벡터/도형 회전) **문장부터 분리**해서 기술.

---

## 13. 종합 실습 — 한 번에 검증

```python
import numpy as np

# 1. 무작위 가역 B, C 생성 (난수 후 QR로 직교기저, 거기서 스케일 변경)
rng = np.random.default_rng(1)
Q1, _ = np.linalg.qr(rng.normal(size=(3,3)))
Q2, _ = np.linalg.qr(rng.normal(size=(3,3)))
S1 = np.diag([2., 1., 0.5])
S2 = np.diag([1.5, 1., 3.])
B = Q1 @ S1
C = Q2 @ S2

# 2. 랜덤 벡터 x 표준좌표 -> B, C 좌표
xE = rng.normal(size=3)
xB = np.linalg.inv(B) @ xE
xC = np.linalg.inv(C) @ xE

# 3. P_{C<-B}로 좌표 이동: [x]_C' = P_{C<-B}[x]_B  (== xC 이어야 함)
PC_B = np.linalg.inv(C) @ B
xC2 = PC_B @ xB
print("xC == P_{C<-B} xB ?", np.allclose(xC, xC2, atol=1e-12))

# 4. 선형변환 A (무작위), A_B, A_C 상호 일치성 검증
A = rng.normal(size=(3,3))
AB = np.linalg.inv(B) @ A @ B
AC = np.linalg.inv(C) @ A @ C

# 관계식: AC = P_{B<-C} AB P_{C<-B}
PB_C = np.linalg.inv(B) @ C
AC2 = PB_C @ AB @ PC_B
print("A_C consistent via similarity? ", np.allclose(AC, AC2, atol=1e-12))

# 5. 고윳값 불변
print("eig(A)=", np.sort_complex(np.linalg.eigvals(A)))
print("eig(A_B)=", np.sort_complex(np.linalg.eigvals(AB)))
print("eig(A_C)=", np.sort_complex(np.linalg.eigvals(AC)))
```

---

## 14. 실무 시나리오 아이디어

- **(그래픽스)** 로컬→월드→카메라 좌표: 뼈대(B)에 붙은 점 좌표를 카메라(C) 좌표로 바꾸기.
- **(데이터)** 원 특성기저 → PCA 기저: 상관 제거·시각화·회귀 안정화.
- **(제어/물리)** 모달기저(고유벡터)로 상태공간 방정식 대각/블록대각화.
- **(수치선형)** 풀기 어려운 방정식을 “좋은 기저”에서 풀고 다시 원기저로 되돌리기.

---

## 15. 요약 표

| 주제 | 공식 | 코멘트 |
|---|---|---|
| 좌표변환(B→C) | \([\boldsymbol x]_C = \underbrace{C^{-1}B}_{P_{C\leftarrow B}}[\boldsymbol x]_B\) | \(x=B[x]_B=C[x]_C\)에서 유도 |
| 역변환 | \([\boldsymbol x]_B = P_{B\leftarrow C}[\boldsymbol x]_C = B^{-1}C[\boldsymbol x]_C\) | \(P_{B\leftarrow C}=(P_{C\leftarrow B})^{-1}\) |
| 변환행렬 표준형 | \(A_B = B^{-1}AB\) | 표준기저 \(A\)→기저 \(B\) |
| 두 기저 간 | \(A_C = P_{B\leftarrow C} A_B P_{C\leftarrow B}\) | 유사변환(고윳값 불변) |
| 직교기저 | \(B^{-1}=B^\top\) | 수치안정·해석 용이 |
| Gram 행렬 | \(G_B=B^\top B\) | 비정규기저일 때 내적표현 |

---

## 16. 마무리 코멘트

- **기저 변화는 표현의 문제**다. “문제를 잘 푸는 좌표계”로 옮기면
  복잡하던 연산이 놀라울 만큼 단순해진다(대각화, PCA, 모달해석, 정규직교화 등).
- 수식은 간단하지만, **행렬의 위치와 역/전치의 방향**을 틀리기 쉽다.
  항상 \(\boldsymbol x=B[\boldsymbol x]_B=C[\boldsymbol x]_C\)에서 시작해 차근차근 유도하자.

---

## 부록: 간단 실습 — 질문에서 든 예제를 코드로

> 질문의 예제: \(B=\big[\,[1,1]^\top,\ [1,-1]^\top\,\big]\), \(\boldsymbol x=[2,0]^\top\).

```python
import numpy as np

x = np.array([2., 0.])

b1 = np.array([1., 1.])
b2 = np.array([1.,-1.])
B  = np.column_stack((b1, b2))

x_B = np.linalg.inv(B) @ x
print("[x]_B =", x_B)   # 기대값: [1, 1]^T

# 검증
print("B [x]_B =", B @ x_B, "== x ?", np.allclose(B @ x_B, x))
```
