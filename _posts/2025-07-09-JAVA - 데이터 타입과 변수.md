---
layout: post
title: Java - 데이터 타입과 변수
date: 2025-07-09 19:20:23 +0900
category: Java
---
# Java의 데이터 타입과 변수

## 변수(Variable)의 선언과 초기화

- **정적 타입 언어**: 변수 선언 시 **자료형**이 필요합니다.
- **선언과 초기화**는 분리 가능하지만, **지역 변수는 초기화 없이 사용 불가**(컴파일 오류).

### 문법과 예시

```java
// 선언만
int count;

// 선언 + 초기화
int age = 25;
double pi = 3.14159;
char grade = 'A';
String name = "Alice";

// 나중에 초기화
count = 10;

// 오류 예: 지역 변수 미초기화 사용
int x;
System.out.println(x); // 컴파일 오류: 변수 x는 초기화되지 않았습니다.
```

### 변수의 유효범위(스코프)

- **블록 스코프**: `{ ... }` 내부에서만 유효
- **이름 충돌 방지**: 바깥 변수와 **동일 이름**의 지역 변수는 **섀도잉**(shadowing) 발생
```java
int v = 1;
{
    int v = 2; // 바깥 v를 가림(섀도잉)
    System.out.println(v); // 2
}
System.out.println(v); // 1
```

---

## 데이터 타입 분류

### 기본형(Primitive Types)

| 타입    | 비트 | 범위(2의 보수) / 값 | 기본값     | 비고 |
|---------|-----:|----------------------|------------|-----|
| `byte`  | 8    | -128 ~ 127           | `0`        | 정수 |
| `short` | 16   | -32768 ~ 32767       | `0`        | 정수 |
| `int`   | 32   | -2,147,483,648 ~ 2,147,483,647 | `0` | **기본 정수** |
| `long`  | 64   | 약 ±9.22e18          | `0L`       | `L` 접미사 |
| `float` | 32   | 단정밀도(IEEE 754)    | `0.0f`     | `f` 접미사 |
| `double`| 64   | 배정밀도(IEEE 754)    | `0.0`      | **기본 실수** |
| `char`  | 16   | `'\u0000'` ~ `'\uffff'` | `'\u0000'` | **UTF-16 코드 유닛**, 부호 없음 |
| `boolean`| *JLS상 크기 비명시* | `true/false` | `false` | 구현 의존 저장 |

> `char`는 **문자**가 아니라 **UTF-16 코드 유닛**입니다(서로게이트/보조 평면 주의).

수식(정수 범위 개념):
$$
\text{int 범위} = [-2^{31},\;2^{31}-1], \qquad
\text{long 범위} = [-2^{63},\;2^{63}-1]
$$

### 참조형(Reference Types)

- **배열**(`int[]`, `String[]`)
- **클래스**(`String`, 사용자 정의 클래스)
- **인터페이스/열거형/레코드/기타**
- 변수 값은 **객체의 참조(주소)**. 기본값은 **`null`** (필드/배열일 때)

---

## 변수의 종류(선언 위치 기준)

| 종류          | 선언 위치              | 수명/특성 |
|---------------|------------------------|-----------|
| **지역 변수**   | 메서드/블록 내부        | 블록 종료 시 소멸, 초기화 필수 |
| **인스턴스 변수** | 클래스 내부(메서드 밖)  | 객체마다 별도 존재, 기본값 자동 부여 |
| **클래스 변수**   | `static` 필드           | 클래스 로딩 동안 1개 공유, 기본값 자동 부여 |

```java
class Counter {
    static int globalCount; // 클래스 변수(기본값 0)
    int perObject;          // 인스턴스 변수(기본값 0)

    void inc() {
        int local = 0;      // 지역 변수(기본값 없음 → 초기화 필요)
        local++;
        perObject++;
        globalCount++;
    }
}
```

---

## 리터럴(Literal)

### 정수 리터럴

```java
int dec = 10;        // 10진수
int bin = 0b1010;    // 2진수(7+)
int oct = 012;       // 8진수
int hex = 0xA;       // 16진수

long big = 10_000_000_000L; // 언더스코어로 가독성 향상, long은 L 필수
```
- **언더스코어 규칙**: 시작/끝/소수점 옆/타입 접미사 앞에는 둘 수 없음.

### 실수 리터럴

```java
double d1 = 2.718;
double d2 = 1.23e3;    // 1.23 × 10^3
float  f1 = 3.14f;     // float은 f/F 접미사
double nan = Double.NaN, inf = Double.POSITIVE_INFINITY;
```
- 부동소수점은 **이진 표현 오차**가 존재합니다(돈 계산은 `BigDecimal` 권장).

### 문자/문자열 리터럴

```java
char c1 = 'A';
char c2 = '\n';           // 이스케이프
char c3 = '\uAC00';       // 유니코드 리터럴: '가'
String s = "Hello";
String t = """
           여러 줄의
           텍스트 블록 (15+)
           """;
```

### 논리 리터럴

```java
boolean flag = true;
```

---

## 형변환(Type Casting)과 수치 승격(Numeric Promotion)

### 자동 형변환(확대 변환)

- 작은 타입 → 큰 타입은 **자동** (안전)
```java
int i = 100;
long L = i;    // OK
float f = L;   // OK (정밀도 손실 가능성은 있지만 문법상 자동)
```

### 강제 형변환(축소 변환)

- 큰 타입 → 작은 타입은 **명시적 캐스트** 필요, **데이터 손실/오버플로** 가능
```java
double d = 3.14;
int n = (int) d;  // 3 (소수부 손실)

int over = (int) 3_000_000_000L; // 오버플로로 음수 등 예기치 값
```

### 산술 변환 규칙(중요)

- **정수 연산**: `byte/short/char`는 연산 시 **`int`로 승격**
- **혼합 연산**: 더 **넓은 타입**으로 승격
- **복합 대입**(`+=` 등)은 내부적으로 캐스트 수행
```java
byte b = 1;
// b = b + 1;    // 컴파일 오류: int 결과를 byte에 대입
b += 1;          // OK: 내부 캐스트

char c = 'A';
int x = c + 1;   // 'A'(65) + 1 → 66
```

---

## 상수와 `final`

### 상수 선언

```java
final double PI = 3.14159;
```
- **재할당 불가**, 관례적으로 **대문자 + 언더스코어** 사용

### 진짜 “컴파일 상수”

- `public static final` + **상수식**(primitive/`String` 조합) → **컴파일 타임 상수**
```java
class Consts {
    public static final int BUF = 8192;    // 컴파일 상수
    public static final String NAME = "APP";
}
```

### effectively final (람다/내부 클래스)

```java
void run() {
    int base = 10; // 변경하지 않으면 effectively final
    Runnable r = () -> System.out.println(base); // 캡처 가능
    // base++; // 주석 해제 시 캡처 불가
}
```

---

## 명명 규칙과 가독성

- **식별자**: 문자/숫자/`_`/`$` (숫자로 시작 불가), **예약어 금지**
- **관례**: `camelCase`(변수/메서드), `PascalCase`(클래스), `UPPER_SNAKE_CASE`(상수)
- **의미 있는 이름**: 역할이 드러나는 이름 사용(줄임말 남용 지양)

---

## Wrapper 클래스와 오토박싱/언박싱

### 対응 관계

| 기본형 | Wrapper |
|---|---|
| `int` | `Integer` |
| `long`| `Long` |
| `double`| `Double` |
| `float`| `Float` |
| `char`| `Character` |
| `boolean`| `Boolean` |
| `short`| `Short` |
| `byte`| `Byte` |

### 예시와 함정

```java
int a = 10;
Integer b = a;        // 오토박싱
int c = b;            // 언박싱

Integer z = null;
// int k = z;         // NullPointerException (언박싱 시 주의)

// == vs equals
Integer i1 = 128, i2 = 128;
System.out.println(i1 == i2);      // false (캐시 범위 밖, 참조 비교)
System.out.println(i1.equals(i2)); // true (값 비교)

Integer i3 = 127, i4 = 127;
System.out.println(i3 == i4);      // true (Integer 캐시 -128~127)
```
- 컬렉션/제네릭은 **참조형**만 허용 → 기본형은 **오토박싱** 발생(성능/메모리 고려)
- **언박싱 NPE** 방지: `Objects.requireNonNull`, `Optional` 또는 기본형 사용

---

## 문자열과 유니코드(문자 처리의 핵심)

### `String` 불변(immutable)

- 덧셈(+) 반복은 **임시 객체**를 많이 생성 → 성능 이슈 시 `StringBuilder` 사용
```java
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 1000; i++) sb.append(i);
String s = sb.toString();
```

### 코드 포인트와 `char`

- `char`는 **UTF-16 코드 유닛** 한 개(2바이트) → **보조 평면 문자**는 2개 `char`로 표현
```java
String emoji = "😺"; // U+1F63A
System.out.println(emoji.length()); // 2 (서로게이트 쌍)
int codePoint = emoji.codePointAt(0);
System.out.println(codePoint); // 128570
```

### 비교와 해시

- 문자열 비교는 **`equals`** 사용, **`==` 금지**(참조 비교)
- **상수 폴딩/인터닝**: 컴파일 상수 문자열은 풀에 인턴됨

---

## 배열(Array)과 가변인자(Varargs)

### 배열 기초

```java
int[] a = new int[3];      // {0,0,0}
int[] b = {1,2,3};
System.out.println(a.length);

int[][] m = new int[2][3];  // 2×3
int[][] jag = new int[2][]; // 톱니형 배열
jag[0] = new int[1];
jag[1] = new int[3];
```
- **경계 검사**: 잘못된 인덱스는 `ArrayIndexOutOfBoundsException`
- 배열은 **참조형** → 기본값은 **필드/배열일 때 자동**(지역 변수는 X)

### 가변인자

```java
int sum(int... xs) {
    int s = 0;
    for (int x : xs) s += x;
    return s;
}
sum(1); sum(1,2,3); sum(new int[]{4,5});
```
- 내부적으로 **배열 생성** → 빈번 호출 시 오버헤드 고려

---

## `var`(지역 변수 타입 추론, Java 10+)

- **지역 변수**에 한정(`var`는 **필드/메서드 시그니처**에 사용 불가)
- **초기화 필수**(우변으로 타입 추론)
- **`null`만** 대입해 선언 불가(타입 추론 불가)
```java
var x = 3;           // int
var y = 3.0;         // double
var s = "text";      // String

// var n = null;     // 컴파일 오류
// var f;            // 컴파일 오류(초기화 필요)

// 람다의 매개변수에는 사용 불가(단, 일부 문법적 예외 존재)
```
- **가독성 우선**: 복잡한 제네릭 우변에서 유리하나, 의미가 불분명하면 명시 타입 권장

---

## 수치 안정성과 오버플로

### 정수 오버플로

- Java 정수는 **모듈러** 래핑(예외 발생 X)
- 검사 필요 시 `Math.addExact`, `subtractExact`, `multiplyExact` 사용
```java
int a = Integer.MAX_VALUE;
int b = a + 1;                          // 음수로 래핑
int c = Math.addExact(a, 1);            // ArithmeticException
```

### 부동소수점 비교

- 이진 부동소수점 오차 → **직접 비교 금지**, **허용 오차(ε)** 사용
```java
double v = 0.1 + 0.2;   // 0.30000000000000004
double eps = 1e-9;
boolean same = Math.abs(v - 0.3) < eps;
```

### 금액/정밀도

- **금융** 등은 `BigDecimal` 권장(스케일/반올림 모드 명시)
```java
import java.math.*;
BigDecimal price = new BigDecimal("19.99");
BigDecimal qty   = new BigDecimal("3");
BigDecimal sum   = price.multiply(qty).setScale(2, RoundingMode.HALF_UP);
```

---

## 기본값과 초기화 순서(필드 기준)

- **필드/배열**: 타입별 **기본값 자동 할당**
- **지역 변수**: **기본값 없음** → 사용 전 반드시 초기화
- **초기화 순서(간략)**:
  1) **정적 필드** 기본값 → 정적 초기화 블록
  2) **인스턴스 필드** 기본값 → 인스턴스 초기화 블록/생성자

```java
class InitOrder {
    static int A = init("static A");
    int B = init("instance B");
    static int init(String s) { System.out.println(s); return 1; }
    InitOrder() { System.out.println("constructor"); }
}
// 출력: static A → instance B → constructor
```

---

## 비교/동등성

- **기본형**: `==`는 **값 비교**
- **참조형**: `==`는 **참조(주소) 비교**, **값 비교는 `equals`**
- **정렬/Map 키**에는 `equals`/`hashCode` 규약 준수 필요

```java
String a = new String("hi");
String b = new String("hi");
System.out.println(a == b);      // false
System.out.println(a.equals(b)); // true
```

---

## 동시성 관점에서의 변수(아주 간략)

- **`volatile`**: 스레드 간 **가시성 보장**(원자성은 단항/참조 읽기·쓰기 수준)
- **`AtomicInteger`** 등 원자 클래스: **복합 연산**의 원자성 제공
- 64비트(`long/double`) 읽기·쓰기의 원자성은 **사양상 보장 대상이 아님**(전통적 JLS 관점) → **`volatile`** 사용 또는 상위 동시성 구조 사용 권장
- **`final` 필드 초기화 안정성**: 생성자 종료 후 다른 스레드에서 안전하게 읽힘

> 실무에서는 고수준 동시성(락, 병행 컬렉션, **가상 스레드(JDK 21+)**)를 우선 고려.

---

## 종합 예제

```java
public class VariableExample {
    // 클래스 상수(컴파일 상수)
    public static final int MAX_USERS = 1000;

    // 클래스 변수(공유)
    static int globalCounter;

    // 인스턴스 변수(객체마다)
    private final String name; // final: 생성자에서 반드시 초기화

    public VariableExample(String name) {
        this.name = name;
    }

    // 가변인자 + 오토박싱 예시(성능 주의)
    static int sum(Integer... xs) {
        int s = 0;
        for (Integer x : xs) {
            if (x != null) s += x; // 언박싱 NPE 방지
        }
        return s;
    }

    static void overflowDemo() {
        int a = Integer.MAX_VALUE;
        int w = a + 1; // 래핑
        System.out.println("wrap: " + w);
        try {
            int e = Math.addExact(a, 1); // 예외
        } catch (ArithmeticException ex) {
            System.out.println("overflow detected");
        }
    }

    public static void main(String[] args) {
        // 기본형/리터럴
        int age = 30;
        final double PI = 3.14159;
        boolean isAdult = age >= 18;
        char initial = 'K';

        // var (지역만)
        var msg = "Hello";
        var n = 3; // int 추론

        // 문자열/유니코드
        String cat = "😺";
        System.out.println(cat + " len=" + cat.length());

        // 배열/가변인자
        int[] arr = {1,2,3};
        System.out.println(sum(1, 2, 3, null));

        // Wrapper 비교 함정
        Integer i1 = 128, i2 = 128;
        System.out.println(i1 == i2);      // false
        System.out.println(i1.equals(i2)); // true

        // 출력
        System.out.println("나이: " + age);
        System.out.println("원주율: " + PI);
        System.out.println("성인? " + isAdult);
        System.out.println("이니셜: " + initial);

        overflowDemo();
    }
}
```

---

## 체크리스트(요약)

- [ ] **지역 변수**는 사용 전 **반드시 초기화**
- [ ] **리터럴/타입 접미사**(`L`, `f`)와 **언더스코어 규칙** 숙지
- [ ] **정수 승격/복합 대입**의 **암시적 캐스트** 이해
- [ ] **오토박싱 NPE**·`==` vs `equals`·`Integer` 캐시(-128~127) 함정 주의
- [ ] **부동소수점 오차** → 비교 시 **ε 사용**, **금융은 BigDecimal**
- [ ] **오버플로** 감시: `Math.addExact` 계열
- [ ] **`final`/컴파일 상수/효과적 final** 구분
- [ ] **`var`는 지역만**, 가독성 우선
- [ ] **문자/코드포인트**(UTF-16)와 문자열 처리 주의
- [ ] **스코프/수명/초기화 순서** 이해(정적→인스턴스→생성자)

---

## 한 줄 요약

> **타입·스코프·수치규칙**을 정확히 이해하면, 자바의 변수와 데이터 모델은 **안전하고 예측 가능**합니다. 실무 품질은 **박싱 함정 회피**, **수치 안정성 확보**, **가독성 있는 선언/명명**에서 갈립니다.
