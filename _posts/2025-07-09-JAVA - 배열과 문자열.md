---
layout: post
title: Java - 배열과 문자열
date: 2025-07-09 22:20:23 +0900
category: Java
---
# Java 배열(Array)과 문자열(String) 처리

## 0. 개요: 언제 배열, 언제 문자열·컬렉션을 쓰나

- **배열**은 고정 길이·동형 데이터에 적합. 인덱스 접근이 O(1), 오토 박싱이 없어 **원시 타입 대량 처리**에 유리.  
- 길이 가변/삽입/삭제가 잦으면 **List/Deque** 등 컬렉션이 적합.  
- **문자열**은 불변(`String`), 빈번한 덧셈은 **`StringBuilder`**로 최적화.  
- 문자 처리 시 **UTF-16**·보조 평면(서로게이트)·로케일 민감 연산에 유의.

---

## 1. 배열(Array)

### 1.1 정의와 성질
- **동형 자료**(같은 타입)의 **연속 메모리 블록**을 인덱스로 접근하는 구조.
- Java에서 배열은 **객체**이며, `length` 필드를 갖고 **경계 검사**가 수행됩니다.
- 인덱스는 **0부터 시작**하며, 범위를 벗어나면 `ArrayIndexOutOfBoundsException`.

```java
int[] a = {1, 2, 3};
System.out.println(a.length); // 3
System.out.println(a[0]);     // 1
// a[3] = 10; // 예외: ArrayIndexOutOfBoundsException
```

### 1.2 선언·생성·초기화 패턴

```java
// 1) 선언 + 리터럴 초기화
int[] nums = {1, 2, 3, 4, 5};

// 2) 선언 후 생성(기본값으로 채움: 숫자 0, boolean false, 참조 null)
String[] names = new String[3];  // {null, null, null}
names[0] = "Kim";

// 3) 익명 배열(메서드 호출 인자에 즉석 생성)
printAll(new int[]{10, 20, 30});

// 4) 정적 초기화 블록
static final int[] LUT;
static {
    LUT = new int[]{1, 4, 9, 16};
}
```

```java
static void printAll(int[] xs) {
    for (int x : xs) System.out.print(x + " ");
    System.out.println();
}
```

> **주의**: `final int[] a`는 **참조** 불변일 뿐, **내용**은 변합니다. 진짜 불변 배열을 원하면 **복사 방어**를 적용하세요.

### 1.3 순회(전통 for vs for-each)와 인덱스
```java
int[] arr = {10, 20, 30};

// 전통 for: 인덱스가 필요하거나 쓰기 가능
for (int i = 0; i < arr.length; i++) {
    arr[i] += 1;
}

// for-each: 읽기 위주(인덱스 없음, 할당은 원소 복사본에)
for (int v : arr) {
    System.out.println(v); // 변경은 arr에 반영되지 않음
}
```

### 1.4 복사·부분 배열·채우기

```java
int[] src = {1,2,3,4,5};

// 1) System.arraycopy (매우 빠른 저수준 복사, 부분 복사/겹침 허용)
int[] dst = new int[5];
System.arraycopy(src, 1, dst, 0, 3); // src[1..3] → dst[0..2]

// 2) Arrays.copyOf / copyOfRange (새 배열 생성)
int[] head3 = java.util.Arrays.copyOf(src, 3);        // {1,2,3}
int[] mid  = java.util.Arrays.copyOfRange(src, 1, 4); // {2,3,4}

// 3) Arrays.fill
java.util.Arrays.fill(dst, -1); // {-1,-1,-1,-1,-1}
```

> **성능 팁**: 전체·부분 복사는 `System.arraycopy`가 가장 빠른 경향. 새 배열이 필요하면 `copyOf`가 간결.

### 1.5 비교/문자열화/탐색/정렬

```java
int[] a = {3,1,2};
int[] b = {3,1,2};

// equals는 **참조 비교**. 내용 비교는 Arrays.equals
System.out.println(a.equals(b));                       // false
System.out.println(java.util.Arrays.equals(a, b));     // true

// toString vs deepToString (다차원일 때 deep 시리즈)
System.out.println(java.util.Arrays.toString(a));      // [3, 1, 2]

// 정렬: 원시 타입은 Dual-Pivot QuickSort, 객체는 TimSort 계열(안정)
java.util.Arrays.sort(a);                              // [1, 2, 3]

// 이진 탐색(정렬 필요)
int pos = java.util.Arrays.binarySearch(a, 2);         // 인덱스 1
```

> **정렬 안정성**: 원시 타입 정렬은 **비안정**, 참조 타입(Comparator/Comparable)은 **안정**.  
> 시간 복잡도(평균) $$O(n \log n)$$, 이진 탐색 $$O(\log n)$$.

### 1.6 다차원/톱니형(jagged) 배열

```java
int[][] rect = { // 직사각형 배열
  {1,2,3},
  {4,5,6}
};
System.out.println(rect[0][1]); // 2

int[][] jag = new int[2][]; // 톱니형(각 행 길이 다름)
jag[0] = new int[1];
jag[1] = new int[3];
```

- `Arrays.deepToString(rect)` / `deepEquals`로 가독성/비교.  
- 행 우선(row-major) 저장. 큰 행·열 순회 시 **캐시 친화적 순서**로 이터레이션 권장.

### 1.7 가변인자(varargs)와 배열의 관계

```java
static int sum(int... xs) { // 내부적으로 int[] 생성
    int s = 0; for (int x : xs) s += x; return s;
}
sum(1); sum(1,2,3); sum(new int[]{4,5});
```

- 빈번 호출·큰 길이에서는 **배열 할당 오버헤드**. 핫패스에서는 명시적 배열 재사용 고려.  
- 오버로드와 **모호성**(예: `int[]` vs `int...`) 주의.

### 1.8 배열 공변성과 제네릭의 불변성(중요 함정)

```java
Number[] ns = new Integer[]{1,2,3};  // 배열은 공변(covariant)
ns[0] = 3.14; // 런타임에 ArrayStoreException

// 제네릭은 불변(invariant). List<Integer>는 List<Number>의 하위 타입 아님.
```

- **배열+제네릭 혼용**은 **힙 오염(heap pollution)** 위험.  
- `new T[]` 생성 불가(타입 소거). 제네릭에는 보통 **List**를 권장, 필요한 경우 `@SafeVarargs`로 호출자 책임을 명확히.

### 1.9 배열→스트림

```java
int[] xs = {1,2,3};
long s1 = java.util.Arrays.stream(xs).asLongStream().sum(); // 박싱 없이
long s2 = java.util.stream.IntStream.of(xs).sum();

String[] ss = {"a","bb","ccc"};
long count = java.util.Arrays.stream(ss).filter(s -> s.length()>1).count();
```

- 원시 스트림(`IntStream`/`LongStream`/`DoubleStream`)은 **박싱 비용 회피**.  
- `parallel()`은 데이터 크기/코어/작업량 등에 따라 체감 성능이 갈림.

---

## 2. 문자열(String)

### 2.1 불변(Immutable)과 힙/풀(Intern Pool)
```java
String a = "Hello";                // 리터럴: 풀 활용
String b = "Hello";
String c = new String("Hello");    // 힙의 별도 객체

System.out.println(a == b);        // true (동일 풀 엔트리)
System.out.println(a == c);        // false (참조 다름)
System.out.println(a.equals(c));   // true (내용 동일)
```

- 불변이라 **스레드 안전**하고 **해시 캐시**가 유리. 다만 빈번한 연결은 임시 객체가 많아져 **비효율** → `StringBuilder`.

### 2.2 주요 API(차이와 함정 포함)

| 메서드 | 설명/주의 |
|---|---|
| `length()` | **UTF-16 코드 유닛 개수**. 코드 포인트 개수와 다를 수 있음 |
| `charAt(i)` | i번째 코드 유닛. 보조 평면 문자는 2유닛 필요 |
| `substring(s,e)` | [s,e) 부분 문자열 |
| `indexOf/lastIndexOf` | 부분 문자열/문자 검색 |
| `startsWith/endsWith` | 접두/접미 비교 |
| `equals/equalsIgnoreCase` | 내용 비교/대소문자 무시(로케일 비독립) |
| `compareTo` | 사전식 비교(유니코드 코드 유닛 순) |
| `replace(char,char)` | 리터럴 치환(정규식 아님) |
| `replaceAll(regex, repl)` | 정규식 기반 치환 |
| `split(regex)` | 정규식 분할(마지막 빈 토큰 제거 규칙·`limit` 주의) |
| `trim()` | ASCII 범위 공백. **유니코드 공백**은 `strip()`(11+) 권장 |
| `toUpperCase/LowerCase(Locale)` | 로케일 지정 권장(예: `Locale.ENGLISH`) |
| `repeat(n)` | Java 11+ |

```java
String s = "  \u2003Hello  "; // \u2003은 유니코드 공백(EM SPACE)
System.out.println(s.trim().length());   // 8 (유니코드 공백 잔존)
System.out.println(s.strip().length());  // 5 (유니코드 공백 제거)
```

### 2.3 문자열 빌더/조인

```java
// 1) StringBuilder (비동기, 빠름)
StringBuilder sb = new StringBuilder(128); // 초기 용량 지정
for (int i = 0; i < 3; i++) sb.append(i).append(",");
String r1 = sb.toString();

// 2) StringBuffer (동기화, 멀티스레드 안전)
StringBuffer buf = new StringBuffer();
buf.append("A").append("B");

// 3) String.join / StringJoiner
String r2 = String.join(", ", "A", "B", "C"); // "A, B, C"
java.util.StringJoiner j = new java.util.StringJoiner(", ", "[", "]");
j.add("x").add("y");
```

### 2.4 유니코드·코드 포인트 처리(고급)

```java
String emoji = "😺"; // U+1F63A (보조 평면)
System.out.println(emoji.length());                 // 2 (서로게이트 2유닛)
int cp = emoji.codePointAt(0);                      // 128570
int count = emoji.codePointCount(0, emoji.length()); // 1

// 코드 포인트 단위 순회
emoji.codePoints().forEach(x -> System.out.printf("U+%X%n", x));
```

- **대소문자/정렬/비교**는 로케일 민감. 사람 친화 비교는 `Collator` 사용.
```java
java.text.Collator col = java.text.Collator.getInstance(java.util.Locale.KOREAN);
System.out.println(col.compare("가", "나")); // 음수(사전순 앞)
```

### 2.5 정규식(Regex)과 `String`

```java
// 이메일 매우 단순 예(실전은 더 정교한 검증 필요)
String email = "a@b.com";
boolean ok = email.matches("[\\w.%-]+@[\\w.-]+\\.[A-Za-z]{2,6}");
System.out.println(ok);

// replace vs replaceAll 차이
System.out.println("a.b".replace(".", "-"));     // "a-b"
System.out.println("a.b".replaceAll("\\.", "-"));// 같은 결과(정규식 사용)

// split의 limit
String line = "a,,b,";
System.out.println(java.util.Arrays.toString(line.split(",")));        // [a, , b]
System.out.println(java.util.Arrays.toString(line.split(",", -1)));    // [a, , b, ]
```

### 2.6 문자열 ↔ 바이트(인코딩)

```java
import java.nio.charset.StandardCharsets;

String t = "한글ABC";
byte[] utf8 = t.getBytes(StandardCharsets.UTF_8); // 인코딩 명시
String t2 = new String(utf8, StandardCharsets.UTF_8);
```

- **항상 문자셋 명시**(`StandardCharsets.UTF_8`). 플랫폼 기본 문자셋 의존은 이식성 저하.

### 2.7 인터닝(intern) — 신중히
```java
String x = new String("abc");
String y = x.intern();
System.out.println("abc" == y); // true
```
- 풀에 저장해 **참조 동일성 재사용** 가능. 하지만 남용하면 **메모리 압박**.

---

## 3. 실전 예제 모음

### 3.1 CSV 한 줄 파서(따옴표/이스케이프 최소 처리 버전)
```java
import java.util.*;

public class CsvLine {
    public static List<String> parse(String line) {
        List<String> out = new ArrayList<>();
        StringBuilder sb = new StringBuilder();
        boolean inQ = false;
        for (int i = 0; i < line.length(); i++) {
            char ch = line.charAt(i);
            if (ch == '"') {
                if (inQ && i + 1 < line.length() && line.charAt(i + 1) == '"') {
                    sb.append('"'); i++; // 이스케이프된 따옴표
                } else inQ = !inQ;
            } else if (ch == ',' && !inQ) {
                out.add(sb.toString()); sb.setLength(0);
            } else sb.append(ch);
        }
        out.add(sb.toString());
        return out;
    }
    public static void main(String[] args) {
        System.out.println(parse("a,\"b,c\",\"d\"\"e\",f"));
        // [a, b,c, d"e, f]
    }
}
```

### 3.2 유니코드 안전한 문자 개수 세기(코드 포인트 기준)
```java
public class CodePointCount {
    public static int lengthInCodePoints(String s) {
        return s.codePointCount(0, s.length());
    }
    public static void main(String[] args) {
        System.out.println(lengthInCodePoints("A😺한")); // 3
    }
}
```

### 3.3 아나그램 판별(정렬/정규식 제거·대소문자 무시)
```java
import java.util.Arrays;

public class Anagram {
    static boolean isAnagram(String a, String b) {
        a = a.replaceAll("\\s+", "").toLowerCase();
        b = b.replaceAll("\\s+", "").toLowerCase();
        int[] ca = a.codePoints().sorted().toArray();
        int[] cb = b.codePoints().sorted().toArray();
        return Arrays.equals(ca, cb);
    }
    public static void main(String[] args) {
        System.out.println(isAnagram("Listen", "Silent")); // true
    }
}
```

### 3.4 배열 집계(박싱 없이 합/평균/최댓값)
```java
import java.util.stream.IntStream;

public class IntStats {
    public static void main(String[] args) {
        int[] xs = {3,1,4,1,5,9};
        int sum = IntStream.of(xs).sum();
        double avg = IntStream.of(xs).average().orElse(0);
        int max = IntStream.of(xs).max().orElse(Integer.MIN_VALUE);
        System.out.println(sum + "," + avg + "," + max);
    }
}
```

### 3.5 안전한 문자열 합치기(빌더/조인)
```java
import java.util.StringJoiner;

public class JoinDemo {
    static String joinCSV(String... xs) {
        StringJoiner j = new StringJoiner(",", "\"", "\"");
        for (String x : xs) j.add(x == null ? "" : x.replace("\"", "\"\""));
        return j.toString();
    }
    public static void main(String[] args) {
        System.out.println(joinCSV("A", "B,C", "D\"E"));
        // "A","B,C","D""E"
    }
}
```

---

## 4. 성능/안정성/보안 관점 팁

1. **문자열 덧셈 루프 금지** → `StringBuilder`/`StringJoiner` 사용.  
2. **배열 대규모 복사** → `System.arraycopy` 선호, 필요 시 **슬라이스 인덱스**만 전달하여 복사 최소화.  
3. **정렬**: 원시타입 vs 객체타입의 정렬 **안정성** 차이 인지. 비교 비용 큰 경우 **키 추출 후 정렬**(Schwartzian transform 방식).  
4. **유니코드**: `length()`는 코드 유닛 수. 사용자 기준 길이는 `codePointCount`. 공백 제거나 대소문자 변환은 `strip()`/`Locale` 명시.  
5. **보안**: 민감 데이터(비밀번호)는 `String`보다 **`char[]`**로 처리 후 **즉시 덮어쓰기**.  
6. **정규식**: 입력 길이 untrusted면 **Catastrophic Backtracking** 방지(패턴 설계·타임아웃·프리미티브 파서 고려).  
7. **가변인자**: 빈번 호출 시 힙 할당. **핫패스**에서는 오버로드·전용 메서드로 회피.  
8. **배열+제네릭** 혼용 지양: `ArrayStoreException`/힙오염. **List/Stream** 우선.

---

## 5. 시간 복잡도(요약, 평균 기준)

- **인덱스 접근**: $$O(1)$$  
- **선형 탐색**: $$O(n)$$  
- **정렬**: $$O(n \log n)$$  
- **이진 탐색(정렬 가정)**: $$O(\log n)$$

---

## 6. 체크리스트

- [ ] 배열 범위/경계 검사, `length` 사용 습관화  
- [ ] 내용 비교는 `Arrays.equals/deepEquals`  
- [ ] 새 배열 필요 여부 판단 → `copyOf/copyOfRange`  
- [ ] 원시 타입은 **박싱 회피**(원시 스트림 사용)  
- [ ] 문자열 처리 시 **로케일/유니코드** 고려 (`strip`, `toUpperCase(Locale)`)  
- [ ] 덧셈 루프 대신 **빌더/조인**  
- [ ] 정규식 `split/replace(All)` 차이·`limit` 파라미터 숙지  
- [ ] 보조 평면 문자(이모지 등) → 코드 포인트 API 사용  
- [ ] 제네릭과 배열 혼용 주의(가능하면 **리스트**)

---

## 7. 요약

- **배열**: 고정 길이·원시 데이터 대량 처리에 강력, 복사/정렬/탐색 유틸을 숙지하면 안전하고 빠르게 다룰 수 있습니다.  
- **문자열**: 불변·풀·유니코드 이해가 핵심. **빌더/조인**, **로케일·정규식·인코딩**을 올바르게 사용하면 성능·정확성·이식성을 모두 잡을 수 있습니다.