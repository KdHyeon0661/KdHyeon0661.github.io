---
layout: post
title: 파이썬 - 클래스 (1)
date: 2024-08-11 19:20:23 +0900
category: Python
---
# 파이썬 클래스 기초 – 클래스의 구조와 개념

## 클래스 기본 구조 (복습)

```python
class 클래스이름:
    def __init__(self, 인자들):
        self.속성 = 인자     # 인스턴스(자신)에 상태 기록
    def 메서드(self, 기타인자):
        # 동작 수행
        ...
```

- **class**: 관련 데이터(속성)와 동작(메서드)을 묶는 사용자 정의 자료형
- **`__init__`**: 생성자(인스턴스 초기화). 반환값 없음(`None`).
- **`self`**: “현재 인스턴스 자신”을 가리키는 1번 인자(관례적 이름).
- **속성(Attribute)**: `self.x` 처럼 인스턴스에 저장되는 데이터(상태).
- **메서드(Method)**: 클래스 내부에 정의된 함수. 첫 인자 `self`(인스턴스 메서드).

---

## 클래스 정의 · 인스턴스 생성

```python
class Person:
    def __init__(self, name, age):
        self.name = name    # 인스턴스 속성
        self.age = age

p1 = Person("Tom", 30)
print(p1.name, p1.age)      # Tom 30
print(type(p1))             # <class '__main__.Person'>
```

- `p1`은 **인스턴스**이고, `Person`은 **클래스(타입)**입니다.

---

## `__init__`와 `self`의 역할

```python
class Car:
    def __init__(self, brand):
        self.brand = brand  # 인스턴스마다 다른 값

    def info(self):
        # self가 있으니 인스턴스 상태에 접근 가능
        print(f"Brand: {self.brand}")

c = Car("Hyundai")
c.info()  # Brand: Hyundai
```

- 인스턴스 메서드는 호출 시 **자동으로 `self`가 전달**됩니다.
- `self`가 정의되지 않으면 `TypeError`가 발생합니다.

```python
class Bad:
    def hello():            # self 없음
        print("Hello")

b = Bad()
# # TypeError: hello() takes 0 positional arguments but 1 was given

```

---

## 인스턴스 속성 vs 클래스 속성

```python
class Cat:
    species = "mammal"   # 클래스 속성(모든 인스턴스가 공유)

    def __init__(self, name):
        self.name = name # 인스턴스 속성(인스턴스마다 다름)

a = Cat("Coco")
b = Cat("Nana")

print(a.species, b.species, Cat.species)  # mammal mammal mammal
print(a.name, b.name)                      # Coco Nana

# 인스턴스에서 클래스 속성과 같은 이름을 할당하면 "가려짐"

a.species = "mutant"
print(a.species, b.species, Cat.species)  # mutant mammal mammal
print(a.__dict__)  # {'name': 'Coco', 'species': 'mutant'}
```

- **클래스 속성**은 **모든 인스턴스가 공유**합니다.
- 같은 이름을 **인스턴스에 직접 대입**하면 그 인스턴스에서만 **가려짐**(Shadowing).

---

## 메서드 3종: 인스턴스/클래스/정적

```python
class MathBox:
    scale = 10

    def mul(self, x):                   # 인스턴스 메서드(첫 인자 self)
        return x * self.scale

    @classmethod
    def set_scale(cls, v):              # 클래스 메서드(첫 인자 cls)
        cls.scale = v

    @staticmethod
    def twice(x):                       # 정적 메서드(self/cls 없음)
        return 2 * x

m = MathBox()
print(m.mul(3))             # 30 (기본 scale=10)
MathBox.set_scale(5)
print(m.mul(3))             # 15 (클래스 속성 변경 영향)
print(MathBox.twice(7))     # 14
```

- **인스턴스 메서드**: 인스턴스 상태가 필요할 때.
- **클래스 메서드**: 클래스 상태(클래스 속성) 조작/대체 생성자 제공.
- **정적 메서드**: 클래스/인스턴스와 무관한 유틸리티 로직.

---

## 캡슐화: 퍼블릭/프라이빗 관례, 네임 맹글링

```python
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner        # 관례상 공개 속성
        self._limit = 1_000_000   # 관례상 내부용(보호) 속성
        self.__secret = "PIN42"   # 네임 맹글링(클래스 내부 전용 의도)

    def get_secret(self):
        return self.__secret

acc = BankAccount("Kim", 100)
print(acc.owner)           # Kim
print(acc._limit)          # 1000000 (접근 가능하지만 "하지 마"라는 신호)
# # AttributeError

print(acc.get_secret())    # PIN42

# 맹글링 실제 이름 확인(권장 X, 디버깅용)

print(acc._BankAccount__secret)    # PIN42
```

- **한 밑줄**: “외부에서 건드리지 마”라는 **약속(관례)**. 문법적 보호는 없음.
- **두 밑줄**: **네임 맹글링**으로 외부 접근을 어렵게 함(보안을 보장하진 않음).

---

## 속성 제어: `@property` (게터/세터/검증/계산 속성)

```python
class Temperature:
    def __init__(self, celsius: float):
        self._c = celsius

    @property
    def celsius(self):              # 읽기
        return self._c

    @celsius.setter
    def celsius(self, value):       # 쓰기 검증
        if value < -273.15:
            raise ValueError("Absolute zero!")
        self._c = value

    @property
    def fahrenheit(self):           # 계산 속성(읽기 전용)
        return self._c * 9/5 + 32

t = Temperature(25)
print(t.celsius, t.fahrenheit)  # 25 77.0
t.celsius = 0
print(t.fahrenheit)             # 32.0
# t.celsius = -300              # ValueError

```

- 함수 호출처럼 보이지 않고 **속성처럼 접근**하면서 **검증/계산** 가능.

---

## 인스턴스 표현/비교: 대표 던더 메서드

### `__repr__`, `__str__`

```python
class Point:
    def __init__(self, x, y): self.x, self.y = x, y
    def __repr__(self): return f"Point(x={self.x}, y={self.y})"  # 개발자/디버깅용
    def __str__(self):  return f"({self.x}, {self.y})"            # 사용자 출력용

p = Point(2, 3)
print(p)          # (2, 3)       → __str__
print([p])        # [Point(x=2, y=3)] → __repr__
```

### 동등성/정렬: `__eq__`, `__lt__` …

```python
class Score:
    def __init__(self, v): self.v = v
    def __eq__(self, other): return isinstance(other, Score) and self.v == other.v
    def __lt__(self, other): return self.v < other.v

s1, s2, s3 = Score(10), Score(10), Score(7)
print(s1 == s2, s1 == s3)   # True False
print(sorted([s1, s3], key=lambda x: x.v))  # [Score(7), Score(10)] (repr 없으니 주소표시)
```

> 던더를 직접 구현하는 대신, **`dataclasses`**(아래 12절)로 자동 생성하는 방법이 실용적입니다.

---

## 상속, `super()`, MRO

```python
class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self): return "<silence>"

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)     # 부모 초기화
        self.breed = breed
    def speak(self): return "Woof"

d = Dog("Choco", "Jindo")
print(d.name, d.breed, d.speak())  # Choco Jindo Woof
```

- **상속**: 공통 로직 재사용, 다형성 실현
- **`super()`**: 부모 초기화/메서드 호출
- **MRO(Method Resolution Order)**: 다중상속 시 메서드 탐색 순서
  ```python
  print(Dog.mro())
  # [<class '__main__.Dog'>, <class '__main__.Animal'>, <class 'object'>]
  ```

> **다중 상속**은 강력하지만 복잡(다이아몬드 상속 문제). 반드시 MRO를 고려해 설계하세요.

---

## vs 상속(Inheritance)

- **상속**: “~이다(is-a)” 관계. Dog는 Animal이다.
- **조합**: “~로 구성되었다(has-a)”. Car는 Engine을 **가진다**.

```python
class Engine:
    def start(self): print("Engine start")

class Car:
    def __init__(self):
        self.engine = Engine()   # 조합
    def drive(self):
        self.engine.start()
        print("Car driving")

Car().drive()
```

> 변하는 축이 다를 때는 **조합**이 보통 더 안전(결합도↓, 재사용↑).

---

## 메모리 최적화: `__slots__`

```python
class Pixel:
    __slots__ = ("x", "y", "color")   # 인스턴스 dict 제거 → 메모리 절감/속도↑
    def __init__(self, x, y, color):
        self.x, self.y, self.color = x, y, color

p = Pixel(1, 2, "red")
# p.z = 10  # AttributeError: 'Pixel' object has no attribute 'z'

```

- 수백만 인스턴스를 만들 때 RAM 차이가 큽니다.
- 단, **동적 속성 추가 불가**, 다중상속/픽클링 등 제약 고려.

---

## 데이터 클래스(`dataclasses`)로 보일러플레이트 제거

```python
from dataclasses import dataclass, field

@dataclass(order=True, slots=True)
class User:
    # order=True → 비교 연산(__lt__ 등) 자동
    # slots=True → 메모리 최적화 (Py3.10+)
    name: str
    age: int = field(default=0, compare=True, repr=True)

u1 = User("Ann", 30)
u2 = User("Bob", 20)
print(u1)          # User(name='Ann', age=30)
print(u1 > u2)     # True (order 기준: name→age 기본은 선언순, order=True면 필드 순서/compare=True 기준)
```

- `__init__`, `__repr__`, `__eq__` 등 자동 생성.
- `field(compare=False, repr=False, default=...)`로 세부 제어.

---

## 컨텍스트 매니저: `__enter__`/`__exit__`

```python
class Resource:
    def __enter__(self):
        print("Acquire")
        return self
    def __exit__(self, exc_type, exc, tb):
        print("Release")
        # 예외를 처리하고 삼킬지 결정: True → 예외 억제
        return False

with Resource() as r:
    print("Using...")
# Acquire
# Using...
# Release

```

- 파일/락/세션 등 **획득-해제**가 필요한 자원에서 필수 패턴.

---

## 고급 생성/소멸: `__new__`, `__del__` (간단 개념)

```python
class OnlyOne:
    _inst = None
    def __new__(cls, *a, **kw):
        if not cls._inst:
            cls._inst = super().__new__(cls)
        return cls._inst
```

- `__new__`: **인스턴스 생성 단계** 제어(싱글톤 등).
- `__del__`: 가비지 컬렉터가 객체 소멸 시 호출될 수 있으나 **의존 금지**(타이밍 비결정적).

---

## 타입 힌트 · Self 타입 (가독성/IDE 지원)

```python
from __future__ import annotations
from typing import Self

class Chain:
    def __init__(self): self.val = 0
    def add(self, x: int) -> Self:
        self.val += x
        return self
    def mul(self, x: int) -> Self:
        self.val *= x
        return self

c = Chain().add(3).mul(5)
print(c.val)  # 15
```

- `Self`(PEP 673): 메서드 체이닝 시 반환 타입을 “자기 자신”으로 명시.

---

## 얕은/깊은 복사: `copy`와 `deepcopy`

```python
import copy

class Box:
    def __init__(self, items): self.items = items

b1 = Box(items=[1, [2, 3]])
b2 = copy.copy(b1)         # 얕은 복사(리스트 참조 공유)
b3 = copy.deepcopy(b1)     # 깊은 복사(내부까지 재귀 복사)

b1.items[1].append(4)
print(b2.items)  # [1, [2, 3, 4]]  ← 공유됨
print(b3.items)  # [1, [2, 3]]
```

---

## 기초

```python
import pickle

class Note:
    def __init__(self, text): self.text = text

n = Note("hello")
data = pickle.dumps(n)
n2 = pickle.loads(data)
print(n2.text)    # hello
```

- 신뢰할 수 없는 소스의 pickle은 **보안상 금지**. 교차 언어/안전 필요하면 **JSON/MessagePack** 등 사용.

---

## 클래스 내부에서 메서드 호출/상태 갱신 (복습 + 패턴)

```python
class Counter:
    def __init__(self, start=0):
        self.count = start
    def increase(self):
        self.count += 1
        self._log()
    def _log(self):
        print(f"현재 값: {self.count}")

c = Counter()
c.increase()  # 현재 값: 1
```

- 내부 동작을 **헬퍼 메서드**로 쪼개면 테스트/확장에 유리합니다.

---

## 작은 프로젝트 예시: 주문/할인 시스템

> 인스턴스/클래스 속성, `@property`, `@classmethod`, `__repr__`, 정렬키, 합계 계산까지 한 번에 복습.

```python
from dataclasses import dataclass, field
from typing import ClassVar, List

@dataclass(slots=True)
class Product:
    name: str
    price: int    # 원화 가정(정수)
    def __repr__(self) -> str:
        return f"Product({self.name!r}, {self.price:,}원)"

class Order:
    tax_rate: ClassVar[float] = 0.1        # 클래스 속성(세율 10%)
    _id_seq: ClassVar[int] = 1

    def __init__(self, products: List[Product]):
        self.id = Order._id_seq
        Order._id_seq += 1
        self._products = list(products)    # 내부 보관
        self._discount = 0.0               # 0.0 ~ 1.0

    @property
    def products(self) -> List[Product]:
        # 외부에서 직접 수정 못 하도록 "복사" 제공(간단 방어)
        return list(self._products)

    @property
    def discount(self) -> float:
        return self._discount

    @discount.setter
    def discount(self, v: float) -> None:
        if not (0.0 <= v <= 1.0):
            raise ValueError("할인은 0.0~1.0 사이")
        self._discount = v

    @property
    def subtotal(self) -> int:
        return sum(p.price for p in self._products)

    @property
    def total(self) -> int:
        after_discount = int(self.subtotal * (1 - self.discount))
        tax = int(after_discount * self.tax_rate)
        return after_discount + tax

    @classmethod
    def set_tax_rate(cls, v: float) -> None:
        if not (0.0 <= v <= 1.0):
            raise ValueError("세율은 0.0~1.0 사이")
        cls.tax_rate = v

    def __repr__(self):
        return f"<Order#{self.id} items={len(self._products)} total={self.total:,}원>"

# 사용

p1 = Product("Book", 15_000)
p2 = Product("Pen",  1_000)
p3 = Product("Bag", 45_000)

o = Order([p1, p2, p3])
print(o, o.subtotal, o.total)    # <Order#1 items=3 total=...> 61000 ...
o.discount = 0.2
print(o, o.total)                # 할인 반영
Order.set_tax_rate(0.08)         # 모든 주문에 적용되는 세율 변경
print(o.total)
```

---

## 실무 체크리스트 (요약)

1. **인스턴스 vs 클래스 속성** 구분: 공통 설정/상태는 클래스, 개별 상태는 인스턴스.
2. **메서드 3종**: 인스턴스/클래스/정적 쓰임새 구분.
3. 데이터 검증/계산 속성은 **`@property`**.
4. 표현/비교는 던더 메서드 또는 **데이터클래스**로 자동화.
5. 상속은 **필요 최소화**. 복잡해지면 **조합** 우선 고려.
6. 대량 인스턴스는 `__slots__` 검토.
7. 자원 관리엔 **컨텍스트 매니저**.
8. 타입힌트/`Self`로 IDE 지원/가독성 향상.
9. 복사/직렬화 동작(얕은/깊은/피클) **명확화**.
10. 프라이빗은 **관례** 수준임(실제 보호는 아님). API 경계 문서화가 중요.

---

## “간단 패턴 모음”

### 대체 생성자: `@classmethod`

```python
class User:
    def __init__(self, name, active=True):
        self.name, self.active = name, active

    @classmethod
    def from_csv(cls, line: str):
        name, active = line.strip().split(",")
        return cls(name, active.lower()=="true")

u = User.from_csv("kim,True")
```

### 불변 객체 흉내(읽기 전용)

```python
class ReadOnlyPoint:
    __slots__ = ("_x", "_y")
    def __init__(self, x, y): self._x, self._y = x, y
    @property def x(self): return self._x
    @property def y(self): return self._y

p = ReadOnlyPoint(1,2)
# p.x = 10  # AttributeError (세터 없음)

```

### 메서드 체이닝

```python
class Builder:
    def __init__(self): self.parts=[]
    def add(self, x): self.parts.append(x); return self
    def build(self): return "-".join(self.parts)

print(Builder().add("A").add("B").build())  # A-B
```

---

## 📌 핵심 정리

- 클래스는 **객체 생성 설계도**. `__init__`은 초기화, `self`는 인스턴스 자신.
- **클래스/인스턴스 속성**을 구분하고, **메서드 3종**을 적재적소에.
- 캡슐화는 `@property`로, 표현/비교는 던더/데이터클래스로.
- 상속은 최소화, **조합 우선**. 자원 관리는 **컨텍스트 매니저**.
- 대량 객체엔 `__slots__`, 타입 힌트/`Self`로 가독성/정확성 향상.
