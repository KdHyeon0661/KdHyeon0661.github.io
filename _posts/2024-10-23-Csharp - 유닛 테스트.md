---
layout: post
title: C# - 유닛 테스트
date: 2024-10-23 19:20:23 +0900
category: Csharp
---
# C# 유닛 테스트와 xUnit

## 유닛 테스트의 중요성과 시작하기

유닛 테스트는 소프트웨어 개발의 품질을 보장하는 핵심 실천법입니다. 코드의 정확성을 검증하고, 리팩토링의 안전성을 제공하며, 문서로서의 역할을 합니다. xUnit은 .NET 생태계에서 가장 널리 사용되는 테스트 프레임워크로, 간결한 문법과 강력한 기능을 제공합니다.

### 프로젝트 설정

```bash
# 솔루션과 프로젝트 생성
dotnet new sln -n MyAppSolution
dotnet new classlib -n MyApp
dotnet new xunit -n MyApp.Tests

# 프로젝트 연결
dotnet sln add MyApp/MyApp.csproj MyApp.Tests/MyApp.Tests.csproj
dotnet add MyApp.Tests reference MyApp

# 필요한 패키지 추가 (템플릿에 포함될 수 있음)
dotnet add MyApp.Tests package xunit
dotnet add MyApp.Tests package xunit.runner.visualstudio
dotnet add MyApp.Tests package Microsoft.NET.Test.Sdk

# 테스트 실행
dotnet test
```

### 권장 프로젝트 구조

```
MyAppSolution/
├── MyApp/
│   ├── Services/
│   ├── Models/
│   └── Utilities/
└── MyApp.Tests/
    ├── Services/
    ├── Models/
    └── Utilities/
```

이러한 구조는 프로덕션 코드와 테스트 코드 간의 명확한 분리를 제공합니다.

---

## xUnit 기본: 테스트 작성의 기초

### AAA 패턴과 명명 규칙

AAA 패턴(Arrange-Act-Assert)은 테스트를 구성하는 표준적인 방법입니다:

1. **Arrange**: 테스트에 필요한 모든 설정을 준비합니다. 객체 생성, 의존성 주입, 초기 데이터 설정 등이 포함됩니다.
2. **Act**: 테스트하려는 동작을 실행합니다. 보통 단일 메서드 호출입니다.
3. **Assert**: 예상된 결과를 검증합니다.

테스트 메서드 이름은 가독성 있게 지어야 합니다. 일반적으로 `메서드명_조건_기대결과` 형식을 따릅니다:

```csharp
[Fact]
public void CalculateTotal_WithValidItems_ReturnsCorrectSum()
{
    // Arrange
    var calculator = new ShoppingCartCalculator();
    var items = new[] { new Item(100), new Item(200), new Item(300) };
    
    // Act
    var total = calculator.CalculateTotal(items);
    
    // Assert
    Assert.Equal(600, total);
}
```

### Fact와 Theory의 구분

`[Fact]` 특성은 단일 테스트 케이스를 정의할 때 사용합니다:

```csharp
[Fact]
public void Add_TwoPositiveNumbers_ReturnsSum()
{
    var calculator = new Calculator();
    var result = calculator.Add(2, 3);
    Assert.Equal(5, result);
}
```

`[Theory]` 특성은 여러 입력 데이터에 대해 같은 테스트를 반복 실행할 때 사용합니다:

```csharp
[Theory]
[InlineData(2, 3, 5)]
[InlineData(10, -5, 5)]
[InlineData(0, 0, 0)]
public void Add_VariousInputs_ReturnsCorrectSum(int a, int b, int expected)
{
    var calculator = new Calculator();
    var result = calculator.Add(a, b);
    Assert.Equal(expected, result);
}
```

### xUnit의 철학: 테스트 격리성

xUnit은 다른 테스트 프레임워크(NUnit, MSTest)와 달리 `[SetUp]`, `[TearDown]` 특성을 제공하지 않습니다. 대신 생성자와 `IDisposable`을 사용하여 테스트 간 격리성을 강제합니다. 각 테스트 메서드 실행마다 테스트 클래스의 새 인스턴스가 생성되므로, 필드에 저장된 상태가 다른 테스트에 영향을 미치지 않습니다. 이 설계는 테스트가 서로 독립적이어야 한다는 **격리성 원칙**을 실현합니다.

```csharp
public class CalculatorTests : IDisposable
{
    private readonly Calculator _calculator;
    
    public CalculatorTests()
    {
        // Arrange (각 테스트 실행 전마다 실행)
        _calculator = new Calculator();
    }
    
    [Fact]
    public void Add_SimpleValues_Calculates()
    {
        var result = _calculator.Add(2, 3);
        Assert.Equal(5, result);
    }
    
    public void Dispose()
    {
        // TearDown (각 테스트 실행 후마다 실행)
        // 필요한 정리 작업 수행
    }
}
```

---

## Assertions: 다양한 검증 방법

### 기본적인 검증

```csharp
// 값 비교
Assert.Equal(expected, actual);
Assert.NotEqual(unexpected, actual);

// 조건 검증
Assert.True(condition);
Assert.False(condition);

// Null 검증
Assert.Null(object);
Assert.NotNull(object);

// 참조 동일성
Assert.Same(expectedReference, actual);
Assert.NotSame(unexpectedReference, actual);
```

### 컬렉션 검증

```csharp
// 컬렉션 포함 여부
Assert.Contains(expectedItem, collection);
Assert.DoesNotContain(unexpectedItem, collection);

// 컬렉션 비교
Assert.Equal(expectedList, actualList);
Assert.NotEqual(unexpectedList, actualList);
```

### 예외 검증

```csharp
// 동기 예외
var exception = Assert.Throws<ArgumentException>(
    () => validator.Validate(null)
);
Assert.Equal("input", exception.ParamName);

// 비동기 예외
await Assert.ThrowsAsync<HttpRequestException>(
    () => httpClient.GetAsync("invalid-url")
);
```

### 범위와 근사값 검증

```csharp
// 범위 검증
Assert.InRange(value, low, high);
Assert.NotInRange(value, low, high);

// 부동소수점 근사값
Assert.Equal(expected, actual, precision: 5);
```

### FluentAssertions 사용하기 (선택 사항)

실무에서는 가독성을 높이기 위해 **FluentAssertions**나 **Shouldly** 같은 외부 라이브러리를 자주 사용합니다.

```bash
dotnet add MyApp.Tests package FluentAssertions
```

```csharp
using FluentAssertions;

[Fact]
public void CalculateTotal_WithValidItems_ReturnsCorrectSum()
{
    // Arrange
    var calculator = new ShoppingCartCalculator();
    var items = new[] { new Item(100), new Item(200), new Item(300) };
    
    // Act
    var total = calculator.CalculateTotal(items);
    
    // Assert
    total.Should().Be(600);
    total.Should().BePositive();
    total.Should().BeGreaterThan(500);
}
```

FluentAssertions는 실패 메시지를 더 이해하기 쉽게 만들어 주고, 다양한 체이닝 메서드를 제공합니다.

---

## 비동기 테스트: 현대적 접근 방식

### 기본 비동기 테스트

```csharp
[Fact]
public async Task LoadDataAsync_ValidUrl_ReturnsData()
{
    // Arrange
    var service = new DataService();
    
    // Act
    var data = await service.LoadDataAsync("https://api.example.com/data");
    
    // Assert
    Assert.NotNull(data);
    Assert.NotEmpty(data);
}
```

### 취소와 타임아웃 처리

```csharp
[Fact]
public async Task ProcessAsync_CancelledToken_ThrowsCancellation()
{
    // Arrange
    var processor = new DataProcessor();
    using var cts = new CancellationTokenSource();
    cts.Cancel();
    
    // Act & Assert
    await Assert.ThrowsAsync<OperationCanceledException>(
        () => processor.ProcessAsync(cts.Token)
    );
}
```

### 비동기 스트림 테스트

```csharp
[Fact]
public async Task GetItemsAsync_ReturnsAllItems()
{
    // Arrange
    var repository = new ItemRepository();
    var expectedItems = new[] { "Item1", "Item2", "Item3" };
    
    // Act
    var items = new List<string>();
    await foreach (var item in repository.GetItemsAsync())
    {
        items.Add(item);
    }
    
    // Assert
    Assert.Equal(expectedItems, items);
}
```

---

## 데이터 주도 테스트: 다양한 데이터 소스 활용

### InlineData를 이용한 간단한 데이터

```csharp
[Theory]
[InlineData(1, 2, 3)]
[InlineData(-1, -1, -2)]
public void Add_SimpleInputs_ReturnsSum(int a, int b, int expected)
{
    var calculator = new Calculator();
    var result = calculator.Add(a, b);
    Assert.Equal(expected, result);
}
```

### MemberData를 이용한 테스트 데이터 분리

```csharp
public class CalculatorTests
{
    public static IEnumerable<object[]> AdditionTestData =>
        new List<object[]>
        {
            new object[] { 1, 2, 3 },
            new object[] { -1, -1, -2 },
            new object[] { int.MaxValue, 0, int.MaxValue }
        };
    
    [Theory]
    [MemberData(nameof(AdditionTestData))]
    public void Add_VariousInputs_ReturnsCorrectSum(int a, int b, int expected)
    {
        var calculator = new Calculator();
        var result = calculator.Add(a, b);
        Assert.Equal(expected, result);
    }
}
```

데이터가 복잡해지거나 여러 테스트 클래스에서 재사용해야 한다면 별도 클래스로 분리하는 것이 좋습니다.

```csharp
public static class TestData
{
    public static IEnumerable<object[]> AdditionData =>
        new List<object[]>
        {
            new object[] { 1, 2, 3 },
            new object[] { -1, -1, -2 }
        };
}

public class CalculatorTests
{
    [Theory]
    [MemberData(nameof(TestData.AdditionData), MemberType = typeof(TestData))]
    public void Add_VariousInputs_ReturnsCorrectSum(int a, int b, int expected)
    {
        // ...
    }
}
```

### ClassData를 이용한 재사용 가능한 테스트 데이터

```csharp
public class MultiplicationTestData : IEnumerable<object[]>
{
    public IEnumerator<object[]> GetEnumerator()
    {
        yield return new object[] { 2, 3, 6 };
        yield return new object[] { 4, 5, 20 };
    }
    
    IEnumerator IEnumerable.GetEnumerator() => GetEnumerator();
}

[Theory]
[ClassData(typeof(MultiplicationTestData))]
public void Multiply_VariousInputs_ReturnsCorrectProduct(int a, int b, int expected)
{
    var calculator = new Calculator();
    var result = calculator.Multiply(a, b);
    Assert.Equal(expected, result);
}
```

---

## 테스트 픽스처: 리소스 공유와 관리

### 클래스 수준 픽스처 (IClassFixture)

데이터베이스 연결이나 파일 시스템 같은 비용이 큰 리소스를 여러 테스트에서 공유할 때 사용합니다:

```csharp
public class DatabaseFixture : IDisposable
{
    public DatabaseConnection Connection { get; }
    
    public DatabaseFixture()
    {
        Connection = new DatabaseConnection("TestDatabase");
        Connection.Open();
        InitializeTestData();
    }
    
    private void InitializeTestData() { /* ... */ }
    
    public void Dispose()
    {
        Connection.Close();
        Connection.Dispose();
    }
}

public class UserRepositoryTests : IClassFixture<DatabaseFixture>
{
    private readonly DatabaseFixture _fixture;
    
    public UserRepositoryTests(DatabaseFixture fixture)
    {
        _fixture = fixture;
    }
    
    [Fact]
    public void GetUser_ExistingId_ReturnsUser()
    {
        var repository = new UserRepository(_fixture.Connection);
        var user = repository.GetUser(1);
        Assert.NotNull(user);
    }
}
```

### 컬렉션 수준 픽스처 (ICollectionFixture)

여러 테스트 클래스에서 동일한 리소스를 공유해야 할 때 사용합니다:

```csharp
[CollectionDefinition("Database collection")]
public class DatabaseCollection : ICollectionFixture<DatabaseFixture>
{
    // 빈 클래스
}

[Collection("Database collection")]
public class UserServiceTests
{
    private readonly DatabaseFixture _fixture;
    
    public UserServiceTests(DatabaseFixture fixture) => _fixture = fixture;
    
    [Fact]
    public void Test1() { }
}

[Collection("Database collection")]
public class ProductServiceTests
{
    private readonly DatabaseFixture _fixture;
    
    public ProductServiceTests(DatabaseFixture fixture) => _fixture = fixture;
    
    [Fact]
    public void Test2() { }
}
```

### IAsyncLifetime: 비동기 초기화/정리

생성자에서는 `async` 코드를 사용할 수 없으므로, 비동기 설정이 필요할 때 `IAsyncLifetime` 인터페이스를 구현합니다.

```csharp
public class IntegrationTests : IAsyncLifetime
{
    private HttpClient _client;
    private TestServer _server;
    
    public async Task InitializeAsync()
    {
        // 비동기 초기화 (테스트 실행 전)
        _server = new TestServer(new WebHostBuilder().UseStartup<Startup>());
        _client = _server.CreateClient();
        await SeedDatabaseAsync();
    }
    
    private async Task SeedDatabaseAsync()
    {
        // 데이터베이스 시딩
    }
    
    public async Task DisposeAsync()
    {
        // 비동기 정리 (테스트 실행 후)
        _client?.Dispose();
        _server?.Dispose();
    }
    
    [Fact]
    public async Task TestMethod()
    {
        var response = await _client.GetAsync("/api/values");
        response.EnsureSuccessStatusCode();
    }
}
```

---

## 테스트 출력과 진단 정보

테스트 실행 중에 추가 정보를 출력할 수 있습니다:

```csharp
public class OutputTests
{
    private readonly ITestOutputHelper _output;
    
    public OutputTests(ITestOutputHelper output)
    {
        _output = output;
    }
    
    [Fact]
    public void TestWithDetailedOutput()
    {
        _output.WriteLine($"테스트 시작 시간: {DateTime.Now:HH:mm:ss.fff}");
        
        var processor = new DataProcessor();
        var result = processor.Process("test data");
        
        _output.WriteLine($"처리 결과: {result}");
        
        Assert.NotNull(result);
    }
}
```

---

## 목(Mock)과 테스트 더블(Test Doubles)

### 의존성 추상화의 중요성

테스트 가능한 코드를 작성하기 위해서는 의존성을 추상화해야 합니다:

```csharp
public interface IEmailService
{
    Task SendAsync(string to, string subject, string body);
}

public interface IDateTimeProvider
{
    DateTime UtcNow { get; }
}

// 실제 구현 생략...

public class NotificationService
{
    private readonly IEmailService _emailService;
    private readonly IDateTimeProvider _dateTimeProvider;
    
    public NotificationService(IEmailService emailService, IDateTimeProvider dateTimeProvider)
    {
        _emailService = emailService;
        _dateTimeProvider = dateTimeProvider;
    }
    
    public async Task SendWelcomeEmail(string email)
    {
        var currentDate = _dateTimeProvider.UtcNow;
        var subject = $"환영합니다! ({currentDate:yyyy-MM-dd})";
        var body = "서비스에 가입해 주셔서 감사합니다.";
        
        await _emailService.SendAsync(email, subject, body);
    }
}
```

### Moq를 이용한 목킹 (기본)

```csharp
[Fact]
public async Task SendWelcomeEmail_CallsEmailService()
{
    // Arrange
    var mockEmailService = new Mock<IEmailService>();
    var mockDateTimeProvider = new Mock<IDateTimeProvider>();
    
    var fixedDate = new DateTime(2024, 1, 1, 0, 0, 0, DateTimeKind.Utc);
    mockDateTimeProvider.Setup(p => p.UtcNow).Returns(fixedDate);
    
    var service = new NotificationService(mockEmailService.Object, mockDateTimeProvider.Object);
    
    // Act
    await service.SendWelcomeEmail("test@example.com");
    
    // Assert
    mockEmailService.Verify(
        s => s.SendAsync(
            "test@example.com",
            $"환영합니다! ({fixedDate:yyyy-MM-dd})",
            "서비스에 가입해 주셔서 감사합니다."
        ),
        Times.Once
    );
}
```

### 과도한 상호작용 테스트 피하기

위의 예제는 행위 기반 검증(메서드 호출 여부)에 집중하고 있습니다. 하지만 모든 것을 Mock으로 검증하면 구현 세부 사항과 테스트가 강하게 결합되어 리팩토링 시 테스트가 쉽게 깨집니다. 가능하면 **상태 기반 검증**을 우선하고, 꼭 필요한 경우에만 상호작용을 검증하세요.

**좋은 예:** 실제 결과값이나 상태 변화를 검증하는 테스트

```csharp
[Fact]
public void CalculateTotal_AddsPricesCorrectly()
{
    var cart = new ShoppingCart();
    cart.AddItem(new Item(100));
    cart.AddItem(new Item(200));
    
    var total = cart.CalculateTotal();
    
    Assert.Equal(300, total); // 상태 검증
}
```

**나쁜 예:** 모든 내부 호출을 검증하려는 테스트 (과도한 명세)

```csharp
// 아래처럼 모든 의존성의 모든 호출을 검증하면 리팩토링 내성이 떨어집니다.
mockInventory.Verify(x => x.CheckAvailability(101, 2), Times.Once);
mockPayment.Verify(x => x.ProcessPayment(99.99m, It.IsAny<string>()), Times.Once);
mockShipping.Verify(x => x.ScheduleDelivery(..., Times.Once);
mockNotifications.Verify(x => x.SendOrderConfirmation(...), Times.Once);
```

**대안:** 통합 테스트나 실제 결과(예: 주문 완료 객체의 상태)를 검증하는 방식으로 전환하세요.

### 최신 Mocking 라이브러리: NSubstitute

Moq 외에도 **NSubstitute**는 간결한 문법으로 인기를 끌고 있습니다.

```bash
dotnet add MyApp.Tests package NSubstitute
```

```csharp
using NSubstitute;

[Fact]
public async Task SendWelcomeEmail_CallsEmailService()
{
    // Arrange
    var emailService = Substitute.For<IEmailService>();
    var dateTimeProvider = Substitute.For<IDateTimeProvider>();
    var fixedDate = new DateTime(2024, 1, 1, 0, 0, 0, DateTimeKind.Utc);
    
    dateTimeProvider.UtcNow.Returns(fixedDate);
    
    var service = new NotificationService(emailService, dateTimeProvider);
    
    // Act
    await service.SendWelcomeEmail("test@example.com");
    
    // Assert
    await emailService.Received(1).SendAsync(
        "test@example.com",
        $"환영합니다! ({fixedDate:yyyy-MM-dd})",
        "서비스에 가입해 주셔서 감사합니다."
    );
}
```

NSubstitute는 `Received()`와 같은 자연어에 가까운 API를 제공합니다.

---

## 시간 추상화: TimeProvider (.NET 8+)

.NET 8부터 `TimeProvider` 추상화 클래스가 도입되어 시간 관련 테스트가 훨씬 쉬워졌습니다.

```csharp
// 프로덕션 코드
public class ReminderService
{
    private readonly TimeProvider _timeProvider;
    
    public ReminderService(TimeProvider timeProvider)
    {
        _timeProvider = timeProvider;
    }
    
    public bool ShouldSendReminder(DateTime lastNotification)
    {
        var now = _timeProvider.GetUtcNow();
        return (now - lastNotification.ToUniversalTime()).TotalDays >= 7;
    }
}
```

테스트에서는 `FakeTimeProvider`를 사용할 수 있습니다. (Microsoft.Extensions.TimeProvider.Testing 패키지 필요)

```bash
dotnet add MyApp.Tests package Microsoft.Extensions.TimeProvider.Testing
```

```csharp
using Microsoft.Extensions.Time.Testing;

[Fact]
public void ShouldSendReminder_WhenSevenDaysPassed()
{
    // Arrange
    var fakeTimeProvider = new FakeTimeProvider();
    var service = new ReminderService(fakeTimeProvider);
    
    var lastNotification = new DateTime(2024, 1, 1, 0, 0, 0, DateTimeKind.Utc);
    fakeTimeProvider.SetUtcNow(new DateTime(2024, 1, 8, 0, 0, 0, DateTimeKind.Utc));
    
    // Act
    var result = service.ShouldSendReminder(lastNotification);
    
    // Assert
    Assert.True(result);
}
```

---

## 통합 테스트: WebApplicationFactory 활용

ASP.NET Core 통합 테스트는 `WebApplicationFactory<TEntryPoint>`를 사용하는 것이 표준입니다.

```bash
dotnet add MyApp.Tests package Microsoft.AspNetCore.Mvc.Testing
```

```csharp
public class OrderApiTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly HttpClient _client;
    
    public OrderApiTests(WebApplicationFactory<Program> factory)
    {
        _client = factory.CreateClient();
    }
    
    [Fact]
    public async Task GetOrders_ReturnsOk()
    {
        var response = await _client.GetAsync("/api/orders");
        
        response.StatusCode.Should().Be(HttpStatusCode.OK);
        var orders = await response.Content.ReadFromJsonAsync<List<Order>>();
        orders.Should().NotBeNull();
    }
}
```

테스트용 데이터베이스를 분리하려면 `WebApplicationFactory`를 커스터마이징합니다.

```csharp
public class CustomWebApplicationFactory : WebApplicationFactory<Program>
{
    protected override void ConfigureWebHost(IWebHostBuilder builder)
    {
        builder.ConfigureServices(services =>
        {
            // 실제 DbContext 제거
            var descriptor = services.SingleOrDefault(
                d => d.ServiceType == typeof(DbContextOptions<AppDbContext>));
            if (descriptor != null) services.Remove(descriptor);
            
            // 테스트용 InMemory 데이터베이스 추가
            services.AddDbContext<AppDbContext>(options =>
                options.UseInMemoryDatabase("TestDb"));
        });
    }
}
```

---

## 테스트 조직화와 실행 제어

### 카테고리화와 필터링

```csharp
[Trait("Category", "Integration")]
[Trait("Priority", "High")]
public class IntegrationTests
{
    [Fact]
    public void DatabaseIntegrationTest() { }
}
```

```bash
dotnet test --filter "Category=Integration"
```

### 테스트 건너뛰기

```csharp
[Fact(Skip = "외부 API 문제로 임시 건너뜀")]
public void ExternalApiTest() { }

// 조건부 건너뛰기
[Fact]
public void WindowsSpecificTest()
{
    if (!OperatingSystem.IsWindows())
    {
        throw new SkipException("이 테스트는 Windows에서만 실행됩니다");
    }
}
```

### 병렬 실행 제어

`xunit.runner.json` 파일로 제어합니다.

```json
{
  "parallelizeTestCollections": true,
  "maxParallelThreads": 4
}
```

특정 테스트 컬렉션을 순차 실행하려면 `[Collection]`을 사용합니다.

```csharp
[Collection("NonParallelTests")]
public class DatabaseTests { }
```

---

## 테스트 커버리지 분석

```bash
dotnet test --collect:"XPlat Code Coverage"
dotnet tool install -g dotnet-reportgenerator-globaltool
reportgenerator -reports:**/coverage.cobertura.xml -targetdir:coverage-report -reporttypes:Html
```

커버리지는 품질 지표 중 하나이지만, 높은 커버리지가 반드시 좋은 테스트를 의미하지는 않습니다. 의미 있는 테스트 케이스가 더 중요합니다.

---

## 실용적인 조언: 상태 검증 vs 행위 검증

- **상태 검증**: 메서드 실행 후 객체의 상태나 반환값을 확인합니다. (권장)
- **행위 검증**: 특정 메서드가 호출되었는지 확인합니다. (필요한 경우에만)

**상태 검증 예제** (좋음):
```csharp
[Fact]
public void AddItem_UpdatesTotalPrice()
{
    var cart = new ShoppingCart();
    cart.AddItem(new Item(100));
    Assert.Equal(100, cart.TotalPrice);
}
```

**행위 검증 예제** (필요한 경우에만):
```csharp
[Fact]
public void Save_CallsRepository_WhenDataIsValid()
{
    var repoMock = new Mock<IRepository>();
    var service = new UserService(repoMock.Object);
    service.SaveUser(new User());
    repoMock.Verify(r => r.Insert(It.IsAny<User>()), Times.Once);
}
```

과도한 행위 검증은 테스트를 깨지기 쉽게 만듭니다. 외부 시스템과의 상호작용이 꼭 검증되어야 하는 경우에만 사용하세요.

---

## 결론

유닛 테스트는 단순히 코드를 검증하는 도구를 넘어서, 더 나은 소프트웨어 설계와 개발 문화를 만드는 핵심 요소입니다. xUnit을 효과적으로 사용하기 위한 핵심 원칙을 정리해 보겠습니다:

### 테스트 철학과 접근법

1. **테스트 주도 설계(TDD)**: 테스트를 먼저 작성함으로써 더 나은 API 설계와 구현을 유도하세요.
2. **FIRST 원칙**: Fast, Independent, Repeatable, Self-validating, Timely.
3. **의존성 주입과 추상화**: 외부 의존성은 인터페이스로 추상화하고 테스트 더블로 대체하세요.
4. **테스트 피라미드 준수**: 단위 테스트(많음) > 통합 테스트(중간) > E2E 테스트(적음).

### 기술적 모범 사례

- **의미 있는 테스트 이름**: `MethodName_Scenario_ExpectedResult` 패턴.
- **한 번에 하나의 개념 테스트**: 각 테스트는 하나의 시나리오만 검증하세요.
- **실제 환경 고려**: CI/CD, 병렬 실행, 성능.
- **리팩토링과 유지보수**: 테스트 코드도 프로덕션 코드만큼 중요하게 관리하세요.
- **지속적인 학습과 개선**: 최신 트렌드(TimeProvider, WebApplicationFactory, FluentAssertions, NSubstitute)를 익히고 적용하세요.

유닛 테스트는 단기적으로 시간 투자가 필요하지만, 장기적으로 버그 감소, 리팩토링 용이성, 문서화 효과, 개발자 자신감 향상 등의 가치를 제공합니다. xUnit의 강력한 기능들과 최신 .NET 테스팅 도구들을 잘 활용하여 견고하고 유지보수 가능한 테스트 스위트를 구축하세요.