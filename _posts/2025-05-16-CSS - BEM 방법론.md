---
layout: post
title: CSS - BEM 방법론
date: 2025-05-16 19:20:23 +0900
category: CSS
---
# BEM 방법론으로 클래스 구조화하기

## 핵심 요약

- **목표**: 명확한 네임스페이스로 **충돌 방지**, **재사용성**과 **유지보수성** 극대화
- **형식**:
  - `block` — 독립 컴포넌트
  - `block__element` — 블록 내부의 구성요소
  - `block--modifier` — 스타일/의미 변형 (상태·크기·톤 등)
- **원칙**: **의미 단위**(구성/상태)에만 BEM 계층을 만든다. DOM 중첩 깊이와 **동일하지 않아도** 된다.

---

## 정식 네이밍 규칙 & 표기 확장

```text
.block {}
.block__element {}
.block--modifier {}
.block__element--modifier {}
```

- **케밥 케이스** 권장: `card__image-wrapper--rounded`
- **Element는 독립 사용 금지**: 항상 상위 block 맥락에서만 의미를 가진다.
- **Modifier는 “변형”**: 색/크기/밀도/톤/배치 형태, 또는 의미상 변형(강조, muted 등)

### 선택자 예시

```html
<article class="card card--highlight">
  <header class="card__header">
    <h3 class="card__title">제목</h3>
    <button class="card__action">…</button>
  </header>
  <div class="card__media">
    <img class="card__image" src="cover.jpg" alt="...">
  </div>
  <p class="card__text">내용</p>
</article>
```

```css
.card { /* Block 기본 */ }
.card__header { /* Element */ }
.card--highlight { /* Block 변형 */ }
.card__text--muted { /* Element 변형 */ }
```

---

## Block / Element / Modifier의 **의미 경계**

| 구분 | 질문 | 예 |
|---|---|---|
| Block | 독립적 재사용 단위인가? | `button`, `card`, `modal`, `tabs`, `form` |
| Element | Block에 **의존**하는 내부 구성인가? | `card__title`, `card__media`, `tabs__tab` |
| Modifier | 같은 Block/Element의 **의미/스타일 변형**인가? | `button--primary`, `card--compact`, `card__text--muted` |

> **레이아웃 그리드** 같은 상위 배치를 위해서는 `l-`(layout), 페이지 전용에는 `p-` 접두 등 **계층 네임스페이스**를 병행하면 충돌을 줄일 수 있습니다.

---

## 상태와 변형 설계 — Boolean vs Key/Value

### Boolean Modifier (단일 on/off)

```html
<button class="button button--loading">제출</button>
```
```css
.button--loading { opacity:.7; pointer-events:none; }
```

### Key/Value Modifier (서로 배타적 그룹)

```html
<button class="button button--size-sm">…</button>
<button class="button button--size-lg">…</button>
```
```css
.button--size-sm { padding:.4rem .6rem; }
.button--size-lg { padding:.9rem 1.2rem; }
```

### vs Modifier 구분

- **UI 상태**(open/selected/invalid)는 **접두 `is-`**로 분리하는 팀도 많다.
- BEM 문법으로만 표기해도 되지만, 자바스크립트/테스트 편의상 아래 형태를 권장:

```html
<div class="modal is-open">…</div>           <!-- 상태 -->
<button class="button button--primary">…</button> <!-- 변형 -->
```

```css
.modal { display:none; }
.modal.is-open { display:block; }
```

---

## “여러 블록의 합성”과 **블록 간 관계**

- **블록은 서로 중첩될 수 있다**: 한 블록이 다른 블록의 엘리먼트가 되지 않게 설계
- **교차 의존 회피**: `.a__x .b__y` 스타일은 유지보수 난이도를 급상승시킴 → 대신 **컨테이너(래퍼) 블록** 혹은 **유틸 클래스** 사용

```html
<section class="product">
  <header class="section-header">…</header>  <!-- 별도 block -->
  <div class="card-list">                     <!-- 별도 block -->
    <article class="card">…</article>
  </div>
</section>
```

---

## 반응형/접근성/테마와 BEM

### 반응형 (media / container queries)

```css
.card { display:grid; gap:1rem; }
@container (inline-size>42rem) {
  .card { grid-template-columns: 1fr 2fr; }
}
```
> **BEM 클래스는 그대로** 두고, **반응 조건만 CSS에서 제어**

### 접근성 (A11y)

```html
<button class="button" aria-pressed="false">즐겨찾기</button>
<button class="button is-pressed" aria-pressed="true">즐겨찾기</button>
```
```css
.button.is-pressed { background: #0a7; }
```
- 상태 클래스를 **ARIA 상태와 동기화**(스크린리더 일관성)
- 포커스 링은 **시멘틱하게**:
```css
.button:focus-visible { outline: 2px solid var(--focus); outline-offset: 2px; }
```

### 테마 (CSS 변수 + BEM)

```css
:root { --brand:#2563eb; }
[data-theme="dark"] { --brand:#8b5cf6; }

.button { background: var(--brand); }
.button--ghost { background: transparent; color: var(--brand); }
```

---

## 최신 CSS와의 결합 (`@layer`, `:where/:is`)

```css
@layer reset, base, components, utilities;

/* components 레이어에서 BEM */
@layer components {
  .card { /* … */ }
  .card__title { /* … */ }
  .card--highlight { /* … */ }
}

/* 특이도 제어: :where()는 0 특이도 */
.card :where(.card__action) { opacity:.8; }
```

**장점**: BEM 네임스페이스 + `@layer`로 우선순위 충돌을 최소화하고, `:where()`로 불필요한 특이도 상승 방지.

---

## 자바스크립트와 BEM (Hook 클래스 전략)

- 스타일과 스크립트 **관심사 분리**: DOM 질의는 `data-*` 또는 `js-*` 접두를 사용
```html
<button class="button button--primary" data-js="favorite">즐겨찾기</button>
```
```js
const btn = document.querySelector('[data-js="favorite"]');
btn.addEventListener('click', () => btn.classList.toggle('is-active'));
```

- BEM 클래스는 **스타일 책임**, JS는 **행동/상태 토글** 책임

---

## 자주 쓰는 컴포넌트 템플릿 (HTML + SCSS)

### 버튼

```html
<button class="button button--primary button--size-lg">
  <span class="button__icon" aria-hidden="true">🔍</span>
  <span class="button__label">검색</span>
</button>
```

```scss
.button {
  display:inline-flex; gap:.5rem; align-items:center;
  border:1px solid transparent; border-radius:.75rem;
  padding:.6rem .9rem; font-weight:600; cursor:pointer;
  transition: filter .2s ease, transform .15s ease;

  &__icon { inline-size:1.1em; block-size:1.1em; }

  &--primary { background: var(--brand); color:#fff; }
  &--secondary { background:#e5e7eb; color:#111; }

  &--size-sm { padding:.4rem .6rem; font-size:.875rem; }
  &--size-lg { padding:.8rem 1.2rem; font-size:1.125rem; }

  &:hover { filter:brightness(1.05); }
  &:active { transform: translateY(1px); }
  &:focus-visible { outline:2px solid var(--focus); outline-offset:2px; }
}
```

### 카드

```html
<article class="card card--highlight">
  <header class="card__header">
    <h3 class="card__title">상품명</h3>
    <button class="card__action">…</button>
  </header>
  <img class="card__image" src="cover.jpg" alt="">
  <p class="card__text card__text--muted">설명 텍스트</p>
</article>
```

```scss
.card {
  display:grid; gap:.75rem; padding:1rem; border:1px solid #e5e7eb; border-radius:1rem; background:#fff;

  &--highlight { border-color: var(--brand); box-shadow:0 4px 16px rgba(0,0,0,.08); }

  &__header { display:flex; align-items:center; justify-content:space-between; gap:.75rem; }
  &__title { font-weight:700; }
  &__action { background:none; border:none; cursor:pointer; }
  &__image { width:100%; border-radius:.75rem; object-fit:cover; aspect-ratio:16/9; }
  &__text--muted { color:#6b7280; }
}
```

### 모달

```html
<div class="modal" role="dialog" aria-modal="true" aria-labelledby="m-title">
  <div class="modal__backdrop"></div>
  <div class="modal__panel">
    <header class="modal__header">
      <h2 id="m-title" class="modal__title">알림</h2>
      <button class="modal__close" aria-label="닫기">×</button>
    </header>
    <div class="modal__content">내용입니다.</div>
    <footer class="modal__footer">
      <button class="button button--secondary">취소</button>
      <button class="button button--primary">확인</button>
    </footer>
  </div>
</div>
```

```scss
.modal {
  position:fixed; inset:0; display:none; z-index:1000;

  &.is-open { display:block; }

  &__backdrop { position:absolute; inset:0; background:rgba(0,0,0,.4); }
  &__panel {
    position:absolute; inset:0; margin:auto; inline-size:min(92vw, 640px);
    background:var(--surface, #fff); color:var(--text, #111); border-radius:1rem; padding:1rem;
    display:grid; gap:1rem;
  }
  &__header { display:flex; justify-content:space-between; align-items:center; }
  &__close { background:none; border:none; font-size:1.25rem; }
  &__footer { display:flex; justify-content:flex-end; gap:.5rem; }
}
```

---

## BEM + Sass 자동화 (토큰 → 유틸/모디파이어 생성)

```scss
/* tokens */
$button-sizes: ( sm: (.45rem .7rem .875rem), lg: (.8rem 1.2rem 1.125rem) );

/* 믹스인: size 생성기 */
@mixin button-size($pad-y, $pad-x, $font) {
  padding: $pad-y $pad-x; font-size: $font;
}

/* 모디파이어 자동 생성 */
.button {
  @each $k, $vals in $button-sizes {
    &--size-#{$k} {
      @include button-size(nth($vals,1), nth($vals,2), nth($vals,3));
    }
  }
}
```

> 토큰만 바꾸면 **모든 변형이 일괄 갱신**, 대규모 UI 라이브러리에서 매우 유용합니다.

---

## 리스트/그리드 패턴 (Block vs Layout)

- **레이아웃 컨테이너**는 별도 Block으로 두고, 카드(자식 Block)는 독립 유지

```html
<section class="card-list">
  <article class="card">…</article>
  <article class="card">…</article>
  <article class="card">…</article>
</section>
```

```css
.card-list {
  display:grid; gap:1rem; grid-template-columns:1fr;
  @container (inline-size>48rem) { grid-template-columns:repeat(3, 1fr); }
}
```

> `card-list`는 Block, `card`도 Block. **Element로 엮지 않는다**(결합도 최소화).

---

## 흔한 안티패턴과 대응

| 안티패턴 | 문제 | 해결 |
|---|---|---|
| `.block__element__sub` | Element 중첩 탈선 → 구조 커플링 | 새 element 이름(역할 기준) 또는 내부에 별도 Block 도입 |
| `.a__x .b__y { … }` | 교차 의존 | 상위 래퍼 Block, 유틸리티, `@layer` 우선순위로 해결 |
| Modifier로 상태 남용 | JS 상태와 규칙 혼동 | `is-` 상태 접두 분리 또는 데이터 속성 사용 |
| 깊은 중첩 Sass | 특이도 상승/가독성 저하 | 3~4 단계 이내, `:where()`로 특이도 억제 |
| 전역 유틸 오염 | 충돌 위험 | 유틸은 `u-` 접두, 문서화/테스트, 최소화 |

---

## 진단 체크리스트

- Block은 독립적 재사용 단위인가?
- Element는 Block에 종속적인가? (독립 사용 금지)
- Modifier는 **같은 것의 변형**인가? (새 컴포넌트 아님)
- JS 상태와 스타일 변형을 구분했는가? (`is-` vs `--`)
- 레이아웃/페이지 범위는 별도 네임스페이스(`l-`, `p-`)로 분리했는가?
- 특이도를 낮게 유지했는가? (`@layer`, `:where`)
- 접근성 속성(ARIA)과 상태 클래스가 동기화되는가?

---

## 팀 운영 팁 (린팅/문서/컴포넌트 카탈로그)

- **Storybook**으로 Block/Element/Modifier 변형을 카탈로그화 → 시각 회귀 테스트
- **Stylelint**(BEM 네이밍 룰 플러그인)로 규칙 자동 검증
- **디자인 토큰** → Sass 맵 → BEM 모디파이어 자동 생성 → **단일 진실원천(SSOT)** 유지

---

## 마이그레이션 전략 (무질서 CSS → BEM)

1) 페이지에서 **핵심 컴포넌트** 추출 → Block 후보 식별
2) 중복 선언과 강한 특이도를 BEM 구조로 **정규화**
3) **유틸/레이어**(reset/base/components/utilities)로 우선순위 정돈
4) 스냅샷 테스트(시각 회귀)로 리스크 관리
5) 점진적 전환: 신규 화면부터 BEM 도입, 구 버전과 공존 전략 수립

---

## 실전 예제: “검색 카드 리스트” 미니 페이지

```html
<header class="site-header">
  <h1 class="site-header__brand">Shop</h1>
  <form class="search search--dense" role="search">
    <input class="search__input" placeholder="검색어 입력" aria-label="검색">
    <button class="search__submit button button--primary">검색</button>
  </form>
</header>

<main class="p-results">
  <section class="card-list">
    <article class="card">
      <img class="card__image" src="1.jpg" alt="">
      <h3 class="card__title">상품 A</h3>
      <p class="card__text">설명…</p>
      <div class="card__actions">
        <button class="button button--secondary">장바구니</button>
        <button class="button button--primary">구매</button>
      </div>
    </article>
    <!-- 반복… -->
  </section>
</main>
```

```scss
/* Block: site-header */
.site-header { display:flex; gap:1rem; align-items:center; justify-content:space-between; padding:1rem; }

/* Block: search */
.search { display:flex; gap:.5rem; }
.search__input { flex:1; padding:.6rem .8rem; border:1px solid #e5e7eb; border-radius:.6rem; }
.search__submit { /* button block 재사용 */ }
.search--dense { gap:.25rem; }
.search--dense .search__input { padding:.45rem .6rem; }

/* Page scope: p-results */
.p-results { padding-inline:1rem; }

/* Block: card-list */
.card-list { display:grid; gap:1rem; grid-template-columns:1fr; }
@container (inline-size>60rem) { .card-list { grid-template-columns:repeat(3, 1fr); } }

/* Block: card */
.card {
  display:grid; gap:.6rem; padding:1rem; border:1px solid #e5e7eb; border-radius:1rem; background:#fff;
  &__image { width:100%; aspect-ratio:4/3; object-fit:cover; border-radius:.75rem; }
  &__title { font-weight:700; }
  &__text { color:#374151; }
  &__actions { display:flex; gap:.5rem; }
  &--highlight { border-color: var(--brand); box-shadow: 0 6px 24px rgba(0,0,0,.08); }
}
```

> **재사용/분리/명시성**을 갖춘 BEM 구조: header/search/card-list/card가 **각자 Block**이며, 상호 간 결합도가 낮다.

---

## BEM vs 유틸리티 프레임워크(Tailwind 등)

- **BEM**: 의미 기반, 컴포넌트 재사용에 강함. 디자인 시스템/문서화에 적합.
- **유틸 클래스**: 속성 기반 조합, 빠른 프로토타이핑/일관된 spacing/typography에 강함.
- **하이브리드**: “Block 뼈대는 BEM”, 세부 스페이싱/정렬은 제한된 범위의 **유틸**을 허용 → 생산성과 명시성의 균형.

---

## 요약 표

| 항목 | 가이드 |
|---|---|
| Block | 독립적 UI 단위. 다른 Block에 종속 금지 |
| Element | Block 내부 구성. 독립 사용 금지 |
| Modifier | 같은 대상의 변형(상태·크기·색·톤) |
| 상태 | `is-` 접두 또는 modifier로 일관되게 |
| 네임스페이스 | `l-`(레이아웃), `p-`(페이지), `u-`(유틸) |
| CSS 최신 | `@layer`, `:where/:is`, container queries |
| 접근성 | ARIA와 상태 클래스 동기화, `:focus-visible` |
| 자동화 | Sass 맵/믹스인으로 모디파이어 생성 |
| 테스트 | Storybook + 시각 회귀, Stylelint BEM 룰 |

---

## 빠른 시작 템플릿

```html
<article class="card card--highlight">
  <h3 class="card__title">Title</h3>
  <p class="card__text">…</p>
  <button class="button button--primary">Action</button>
</article>
```

```css
@layer components {
  .card { /* block */ }
  .card__title { /* element */ }
  .card--highlight { /* modifier */ }
}
```

---

## 참고 리소스

- BEM Methodology: https://en.bem.info/methodology/
- Stylelint BEM Patterns: https://github.com/simonsmith/stylelint-selector-bem-pattern
- CSS Cascade Layers: https://developer.mozilla.org/en-US/docs/Web/CSS/@layer
- Container Queries: https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_containment/Container_queries

---

### 마무리

BEM은 **명확한 역할 분리와 재사용성을 보장**하는 가장 실전적인 CSS 설계 방법론 중 하나입니다.
**Block/Element/Modifier**의 의미 경계를 지키고, **상태/테마/반응형/접근성**을 표준 CSS 기능과 합리적으로 결합하면,
규모가 커져도 **읽히고 바뀌는** 스타일 시스템을 만들 수 있습니다.
